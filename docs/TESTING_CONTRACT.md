# 🔬 TESTING_CONTRACT — тестовые контуры проекта

**Основной читатель:** инженер, сопровождающий или развивающий AI Portfolio.
**Назначение:** какие тестовые контуры существуют, что каждый проверяет, как запускать и какие критерии считать приёмкой. Методика и результаты eval-кампаний подробно — [`docs/AI_EVAL_REPORT.md`](AI_EVAL_REPORT.md); сквозные сценарии — [`docs/E2E_SCENARIOS.md`](E2E_SCENARIOS.md).

> 📌 **SOT:** состав и команды соответствуют фактическому состоянию репозитория на 2026-09-03.

---

## 🗺️ 1. Три контура

| Контур | Что проверяет | Где |
|--------|---------------|-----|
| **Unit-набор** (`backend/tests/`) | Логика backend'а: чат-оркестратор, RAG/admission-gate, консоли, синхронизация, индексация, кеш, трассировка — без внешних сервисов | [`backend/tests/`](../backend/tests/) |
| **Eval-прогоны** | Качество и latency живого ассистента через production-пайплайн (retrieval + генерация) | [`docs/AI_EVAL_REPORT.md`](AI_EVAL_REPORT.md), набор `backend/tests/ai_portfolio_eval_set.json` |
| **E2E-сценарии** | Сквозное поведение продукта глазами посетителя и администратора (сайт, чат-виджет, Admin Console) | [`docs/E2E_SCENARIOS.md`](E2E_SCENARIOS.md) |

---

## 🧪 2. Unit-набор

```bash
cd backend && .venv/bin/python -m pytest tests/ -q
```

| Параметр | Значение |
|----------|----------|
| Объём | 247 тестов в 27 файлах |
| Текущий результат | 242 passed, 3 skipped (на хосте вне compose) |
| Smoke-тесты БД | 2 теста `test_services_smoke.py` требуют живого PostgreSQL по адресу compose-сети — в контуре хоста не проходят; в контуре развёрнутой системы проходят штатно |

Покрытие ключевыми модулями (примеры): `test_chat_orchestrator_fixes` — фиксы оркестратора (валидация `session_id`, контекст страницы, honest-отказы); `test_kb_admission_gate`, `test_admission_console_guards` — допуск источников KB (draft/effective-модель, stale-защита, 409-поведение); `test_heading_aware_chunking` — нарезка чанков по заголовкам; `test_indexing_active_backend`, `test_chroma_collection_config` — индексация и конфигурация коллекций; `test_retrieval_wh1_wh2`, `test_rag_recall_margin` — качество retrieval; `test_registry_and_prompt`, `test_system_prompt_console` — реестр и контур промптов; `test_sync_delete_guard`, `test_sync_single_flight` — защита синхронизации; `test_weaviate_schema_tokenization` — токенизация weaviate-классов; `test_presale_console`, `test_documents_console`, `test_retrieval_console` — консоли.

**Критерий приёмки изменений:** зелёный полный набор перед коммитом; красные smoke-тесты БД — ожидаемы только вне compose-окружения.

---

## 🎯 3. Eval-прогоны

Качество ассистента проверяется прогонами через живой пайплайн (`POST /chat`), не моками.

### Наборы

| Набор | Состав | Назначение |
|-------|--------|------------|
| **Публичный eval-сет** | 24 теста, классы A–M (файл `backend/tests/ai_portfolio_eval_set.json`) | Регрессия качества: перечисление кейсов, факты о проектах, диалоговая память, отказы, честность |
| **Полный regression set** | 168 тестов, классы A–M | Контрольная кампания при изменениях промпта/retrieval/KB |
| **Класс X** | 8 «грязных» формулировок (опечатки, транслит, размытые фразы) | Понимание фразы; добавляется к regression set |

### Принятые результаты (28.08.2026, принятые владельцем)

| Метрика | Значение | Критерий |
|---------|----------|----------|
| Корректность (regression) | 159/168 = **94,6%** | ≥90% ✅ |
| Класс C (факты о проектах) | 68/77 = 88,3% (уровень зафиксирован) | — |
| Latency p95 (cache-miss) | **4956 мс** | <5000 мс ✅ |

### Правила прогона (важные)

- Кеш ответов сбрасывается (с бэкапом) перед каждым измерительным прогоном — прогон на прогретом кеше меряет кеш, а не поведение.
- Изолированный `visitor_id` на запуск: eval-трафик не смешивается с реальными посетителями.
- Сессия диалоговой группы якорится: первый запрос группы без `session_id`, последующие — с `session_id` из ответа сервера (сервер валидирует переданный id).
- Класс C стохастически колеблется между прогонами (наблюдаемый диапазон 88,3–90,9%); повторные полные прогоны — только по согласованию с владельцем.

Методика, состав классов, раннер и разборы дефектов — [`docs/AI_EVAL_REPORT.md`](AI_EVAL_REPORT.md) §6–§7.

---

## 🔗 4. E2E-сценарии

Сквозные сценарии продукта фиксируются в [`docs/E2E_SCENARIOS.md`](E2E_SCENARIOS.md): путь посетителя (витрина → чат → источники → панель документа → обращение) и путь администратора (допуск источника, синхронизация, промпт). Проверки выполняются на живой системе; UI-проверки выполняются по фактическому DOM (значения и классы элементов), а не по внешнему виду.

---

## ✅ 5. Критерии готовности релиза

| Критерий | Источник |
|----------|----------|
| Корректность eval ≥90%, latency p95 <5 с | [`docs/AI_EVAL_REPORT.md`](AI_EVAL_REPORT.md) §3 |
| Зелёный unit-набор | §2 этого документа |
| Deployment Validation PASS в чистом окружении | [`docs/DEPLOYMENT_VALIDATION_REPORT.md`](DEPLOYMENT_VALIDATION_REPORT.md) |
| Прогон E2E-сценариев на production | [`docs/E2E_SCENARIOS.md`](E2E_SCENARIOS.md) |

---

## 📚 Связанные документы

- [🧪 `docs/AI_EVAL_REPORT.md`](AI_EVAL_REPORT.md) — методика и результаты eval-кампаний.
- [🎬 `docs/E2E_SCENARIOS.md`](E2E_SCENARIOS.md) — сквозные сценарии продукта.
- [🏗️ `docs/ARCHITECTURE.md`](ARCHITECTURE.md) — архитектура (что тестируется).
- [📡 `docs/API_CONTRACT.md`](API_CONTRACT.md) — контракт эндпойнтов, задействованных в eval.
- [🧾 `docs/DEPLOYMENT_VALIDATION_REPORT.md`](DEPLOYMENT_VALIDATION_REPORT.md) — протокол валидации воспроизводимости.

---

## 📝 История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2026-09-03 | 1.0 | Первая версия: unit-набор (247 тестов), eval-наборы и правила прогонов, E2E, критерии готовности релиза |