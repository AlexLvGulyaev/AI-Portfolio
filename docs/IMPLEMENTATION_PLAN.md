# IMPLEMENTATION_PLAN.md — AI Portfolio: остаточный план до production-ready release

**Проект:** ai-portfolio
**Версия:** 4.4
**Дата:** 2026-08-28
**Статус:** Утверждено (остаточный план от состояния на 28.08.2026)
**Целевая дата production-ready release:** 04.09.2026 (не переносится)

---

## 1. Обзор плана

### 1.1 Что это за план

Настоящая версия — **остаточный план**: все работы, выполненные до 27.08.2026, перенесены в компактный блок `COMPLETED` (§3); план описывает только работы, оставшиеся до production-ready release 04.09.2026. Календарные даты 19–26 августа из предыдущих версий (v3.0–v3.3) больше не считаются будущими работами.

Основной источник фактического состояния — `docs/PROJECT_STATE.md` (27.08.2026).

### 1.2 Исходные документы (SOT)

| Документ | Назначение |
|----------|------------|
| [`docs/TZ.md`](TZ.md) | Техническое задание v1.6 — scope, MoSCoW, критерии приёмки |
| [`docs/PROJECT_STATE.md`](PROJECT_STATE.md) | Состояние проекта на 27.08.2026, целевой состав GitHub Sync, KB admission gate |
| [`docs/SPEC.md`](SPEC.md) | Продуктовая спецификация v2.3 (as-built baseline) |
| [`docs/ARCHITECTURE.md`](ARCHITECTURE.md) | Архитектура и технический стек |
| [`docs/DEPLOYMENT_GUIDE.md`](DEPLOYMENT_GUIDE.md) | Процесс развёртывания |
| [`docs/PORTFOLIO_CORPUS_AUDIT.md`](PORTFOLIO_CORPUS_AUDIT.md) | Матрица технического долга, интеграции и NEW GAP |
| `task_history/2026-08-27_task-aip-current-state-and-release-path.md` | Анализ текущего состояния и путь к релизу |

### 1.3 Состояние на 27.08.2026 (сводка)

- Публичная поверхность **в основном завершена**: главная `/` (одновременно витрина всех проектов), 13 полноценных лендингов кейсов по `AIP Case Landing Page Standard v1.1` (эталон — `/cases/ai-curator.html`), «Услуги», «Контакты», redirect устаревшего каталога на `/`.
- Портфель — **13 полноценных проектов**, включая Prompt Review (finished-case с публичным репозиторием).
- Инфраструктура: FastAPI + PostgreSQL + ChromaDB HTTP на VPS, Admin Console v1, multi-provider LLM (OpenAI + GigaChat), tracing, логи, аудит. Production: `https://ai.alex-n8n.site`.
- KB: **12 источников-репозиториев, 211 документов (PG), 5253 чанка (ChromaDB, collection `ai_portfolio_knowledge_v2`)** — перепроверено по живому состоянию 28.08.2026; старая collection удалена (см. §2).
- Публичный репозиторий `AlexLvGulyaev/AI-Portfolio` уже опубликован и содержит проект (src, backend, admin, docs, docker-compose).
- Оставшийся объём — ядро: Morphic (OWNER DECISION GATE), presale-аналитика, UI управления допуском в Admin Console (§4.5а), тестирование, Deployment Validation, release. KB, допуск источников, retrieval/prompt и основная eval-кампания завершены 28.08 (см. §2, §4.1–4.3).

---

## 2. Статусный реестр работ

### COMPLETED (подтверждено репозиторием и task-файлами 25–27.08)

- Dual-theme главная страница с динамической загрузкой `/project-cards`, featured/archive по `display_order`, переключателем темы, чат-виджетом.
- 13 полноценных страниц кейсов (`src/cases/*.html`) по стандарту `AIP Case Landing Page Standard v1.1`; AI Curator — ручная эталонная страница (`MANUAL_CASES`).
- Prompt Review финализирован как полноценный finished-case (репозиторий `AlexLvGulyaev/PromptReview`, страница кейса).
- Страницы «Услуги» и «Контакты» (mirror-bento) созданы и включены в навигацию.
- Redirect устаревшего каталога: `/portfolio.html` и `/projects` → `/`.
- Backend: FastAPI + PostgreSQL + ChromaDB HTTP; `ProjectCard` как SOT карточек; multi-provider LLM; tracing; operational logs; аудит.
- Admin Console v1 (5 разделов).
- GitHub Sync базового корпуса (7 источников) и начальная индексация ChromaDB.
- Production deploy: `https://ai.alex-n8n.site`.
- Публичный репозиторий `AlexLvGulyaev/AI-Portfolio` (содержит проект; остаточно — ревизия поставки и актуализация документации).

**Дополнение 28.08.2026 (к кампании допуска KB и диагностической eval-кампании):**

- KB admission gate реализован (fail-closed): `backend/app/services/admin/kb_admission.py`, миграция 014 (`admission_status`, `include_patterns`, `exclude_patterns` на `knowledge_sources`), guard'ы sync/delete, тесты — коммит `f899f24`.
- KB переведена на collection `ai_portfolio_knowledge_v2`: 12 источников / 211 документов / 5253 чанка; старая collection `ai_portfolio_knowledge` (5400 чанков) удалена.
- Диагностическая eval-кампания на production (baseline + 5 re-runs, 174 теста/прогон): устранены дефекты provenance, кеша (registry-only policy, KEY_SCHEMA `v4-registry-only`), дрейфа памяти; промпт `v4-compact-multi` с детерминированным реестром 13 проектов. Финал: **159/168 = 94,6%** формально; C-класс после adjudication **68/77 = 88,3% — принят владельцем (решение 28.08, вариант «а»)**; latency production cache-miss p95 4956 мс < 5000 мс.
- Eval-данные (visitor UUID `e7a1c0de-*`) удалены из production БД в транзакции 28.08 (163 сессии / 2284 сообщения / 1142 execution-сессии / 12562 шага); реальные пользовательские записи не затронуты.

### IN PROGRESS

- Актуализация документации отдельных портфельных проектов — **известное ограничение качества источников, не блокировка** завершённой кампании допуска (28.08): не у всех проектов документация доведена до стандартов APL; учитывается механизмом admission gate при будущих допусках.

### NOT STARTED

- Остаток §4.1: решение владельца по orphaned-файлам в `src/cases/` (п.1.6) — сам admission gate реализован 28.08 (коммит `f899f24`).
- Остаток §4.3: формализация eval-результатов — `docs/AI_EVAL_REPORT.md` + обезличенный regression set (публичный; полный benchmark/traces/snapshots/adjudication остаются в private volume) — сама eval-кампания выполнена 28.08, результат принят владельцем.
- **Собственный корпус AI Portfolio (13-й источник) — отложен решением владельца (28.08.2026):** подключение выполняется **после финализации доработки AIP и документации проекта**, непосредственно перед Deployment Validation. Обоснование: документация корпуса активно меняется (индексировать летящий корпус = устаревший индекс), каждый reindex влечёт пересборку коллекции, инвалидацию кеша и повторный eval — цикл «допуск → sync → reindex → eval» должен быть пройден один раз на зрелой документации. Внутренние документы (`PROJECT_STATE.md`, `IMPLEMENTATION_PLAN.md`, аудит долга) в текущем виде в KB не допускаются; состав допуска определяется через admission gate (`include_patterns`/`exclude_patterns`) при подключении.
- Morphic: проектирование UX-контракта (§4.4, статус PLANNED / OWNER DECISION GATE).
- Presale-события и presale-аналитика в Admin Console (§4.5).
- Admin Console: UI управления допуском источников KB — отображение/редактирование `admission_status`, include/exclude-паттернов, просмотр admission-preview (§4.5а; выявлено 28.08, выполняется до §4.6).
- Тестирование: функциональное, E2E, security, graceful degradation (§4.6).
- Deployment Validation в чистом окружении + отчёт (§4.7).
- Ревизия и финализация публичного репозитория `AlexLvGulyaev/AI-Portfolio` (§4.7).

### BLOCKED

- Финализация публичной поставки `AI-Portfolio` — блокирована Deployment Validation (обязательный критерий готовности по правилам APL) и ревизией `.gitignore`/внутренних артефактов.

### NEEDS VERIFICATION

- Работоспособность demo-ссылок отдельных кейсов (Telegram Intake Bot, Telegram Onboarding Bot — demo после Deployment Validation; Review Flow — повторная Validation).
- Известный риск (на рассмотрении владельца, решение 28.08 — пока не реализовывать): retrieval не имеет запасного канала — эмбеддинги ходят только в OpenAI, GigaChat-fallback покрывает только генерацию; варианты решения зафиксированы в `task_history/2026-08-28_task-aip-diagnostic-eval.md`.

---

## 3. COMPLETED — компактный блок выполненных работ (19.08–26.08)

**Уточнение по статусу `EXISTING DEBT` (корректирующая сверка 27.08.2026):** декомпозиция EXISTING DEBT + AIP INTEGRATION по портфельным проектам была выполнена и зафиксирована в планах v3.1–v3.3 и `PORTFOLIO_CORPUS_AUDIT.md`, однако **весь EXISTING DEBT проектов не закрыт**. Завершена публичная интеграция (карточки, 13 страниц кейсов по стандарту AIP v1.1). Документация не всех проектов доведена до стандартов APL. Проектная финализация продолжается и является зависимостью допуска материалов в KB (§4.1). Наличие лендинга кейса не означает production-ready статус проекта; если конкретный статус не доказан, проект описывается нейтрально — «полноценный публичный кейс».

Итоги выполненных работ:

| Работа | Результат |
|--------|-----------|
| SOT-документы (TZ, SPEC, IMPLEMENTATION_PLAN, PROJECT_STATE) | Актуализированы и утверждены (последующие актуализации — см. историю изменений) |
| Реестр проектов и ProjectCard | 13 полноценных карточек в PostgreSQL; SOT карточек |
| Публичная витрина | Главная dual-theme, 13 лендингов по AIP v1.1, услуги, контакты, redirect каталога |
| Стандарт лендинга кейса | `AIP Case Landing Page Standard v1.1` задокументирован в `shared/patterns/`; эталон — `/cases/ai-curator.html` |
| Prompt Review | Полноценный finished-case с публичным репозиторием |
| Инфраструктура | FastAPI + PostgreSQL + ChromaDB HTTP + Admin Console v1 + multi-provider LLM на VPS |
| GitHub Sync (база) | 7 источников, начальный корпус ChromaDB |

---

## 4. Остаточные работы (критический путь до 04.09.2026)

### 4.0 Критическая зависимость

Завершено 28.08.2026 и выведено из будущего критического пути: допуск источников (KB admission gate) → GitHub Sync (12 репозиториев витрины) → ChromaDB reindex (`ai_portfolio_knowledge_v2`) → настройка retrieval/prompt → production eval (**принято владельцем: 159/168 = 94,6%; p95 4956 мс < 5 сек**).

**Текущий остаточный путь:**

1. Решение владельца по orphaned-файлам (§4.1, п.1.6).
2. ~~Формализация eval-результатов (`docs/AI_EVAL_REPORT.md`, обезличенный regression set) (§4.3, остаток).~~ **Выполнено 28.08.2026** (`docs/AI_EVAL_REPORT.md` + `backend/tests/ai_portfolio_eval_set.json`, коммит `fe8a815`).
3. Решение владельца по release scope Morphic (§4.4, PLANNED / OWNER DECISION GATE; Must только после отдельного решения).
4. UI admission gate в Admin Console (§4.5а).
5. Presale-события и аналитика (§4.5).
6. Функциональные и E2E-тесты (§4.6).
7. Финализация документации AI Portfolio.
8. Отдельный допуск собственного корпуса AIP → sync → reindex → один контрольный eval (отложено владельцем; перед Deployment Validation).
9. Deployment Validation (§4.7).
10. Production release 04.09.2026 (§4.7).

Зависимости внутри остаточного пути:

- **Критический Must-путь:** п.1 → 4 → 6 → 7 → 8 → 9 → 10 — каждый шаг блокирует следующий; пропуск или ослабление любого из них делает E2E и Deployment Validation невалидными как доказательства готовности.
- **Параллельные работы:** presale-аналитика (п.5, §4.5) и UI допуска (п.4, §4.5а) не блокируют друг друга и могут выполняться параллельно с подготовительными частями п.6–7.
- **Morphic (п.3) — неблокирующий OWNER DECISION GATE:** до отдельного решения владельца о release scope Morphic не блокирует ни один шаг пути; обязательный presale-путь работает через существующую главную и AI-ассистента (§4.4).

### 4.1 KB admission gate и документация источников — P1, Must, 1–1.5 дня — **COMPLETED 28.08.2026** (остаток: только п.1.6)

**Статус:** реализован (fail-closed, миграция 014, guard'ы sync/delete, тесты — коммит `f899f24`); все 12 источников витрины допущены и синхронизированы (§2, §2.2 PROJECT_STATE). Из п.1.1–1.5 работы выполнены; остаётся только решение владельца по orphaned-файлам (п.1.6).

| # | Работа | Результат |
|---|--------|-----------|
| 1.1 | Для каждого целевого источника: определить фактический репозиторий (реестр — §2 `PROJECT_STATE.md`) | Реестр «кейс → репозиторий → источник» |
| 1.2 | Проверить публичность, актуальность и непротиворечивость документов каждого репозитория | Отчёт проверки по каждому источнику |
| 1.3 | Определить разрешённый состав индексируемых материалов (включая пути/маски) | Whitelist/чёрный список путей на источник |
| 1.4 | Исключить из индексации внутренние, устаревшие и неподтверждённые документы | Настроенные исключения |
| 1.5 | При отсутствии пригодного корпуса — актуализировать минимальный канонический публичный документ проекта | Минимальный канонический документ |
| 1.6 | Решение владельца по orphaned-файлам в `src/cases/` (`ai-curator-template-preview.html`, `competitor-monitor.html`, `lead-qualification-nb.html`, `telegram-ai-gateway.html`): удалить; перенести за пределы публичной сборки; либо явно исключить из процесса сборки/публикации. Примечание: `.gitignore` не исключает уже отслеживаемые Git файлы и не удаляет их из публичной сборки | Зафиксированное решение владельца по каждому файлу |

Оценка 1–1.5 дня относится к самому допуску; актуализация канонических документов при необходимости выполняется по объёму фактически неготовых проектов и **не входит в настоящую задачу как исполнительская работа** — она фиксируется как зависимость и при угрозе сроку выносится на решение владельца (DEFER CANDIDATE утверждает только владелец).

### 4.2 GitHub Sync + ChromaDB reindex — P1, Must, 1–1.5 дня — **COMPLETED 28.08.2026** (12 репозиториев витрины)

**Статус:** 12 проектных репозиториев витрины подключены и синхронизированы в `ai_portfolio_knowledge_v2` — **12 источников / 211 документов / 5253 чанка / 5253 unique ID**, дубликатов 0; корпус верифицирован в процессе допуска (внутренние материалы и документ с секретом в индекс не попали, п.2.4). Собственный корпус AI Portfolio (13-й источник) — отложен решением владельца до финализации документации, перед Deployment Validation (§4.0, шаг 8).

| # | Работа | Зависимость | Результат |
|---|--------|-------------|-----------|
| 2.1 | Расширить GitHub Sync с 7 до целевого состава: **12 проектных репозиториев + собственный корпус AI Portfolio** (включая `Retail-Group`) | 4.1 | Все допущенные источники подключены |
| 2.2 | Индексация `HR-Assistant/finetuning` (HRA-LoRA) отдельным источником или в составе HR-Assistant — фиксируется на этапе допуска | 4.1 | Состав источника HRA-LoRA определён |
| 2.3 | Пересобрать ChromaDB на расширенном корпусе | 2.1 | Актуальный индекс; reindex без дубликатов |
| 2.4 | Верификация корпуса: отсутствие в индексе секретов, персональных данных, внутренних материалов | 2.3 | Контрольный чек-лист пройден |

### 4.3 AI-ассистент: retrieval/prompt + eval — P1/P3, Must, 2–3 дня — **COMPLETED 28.08.2026 / OWNER ACCEPTED** (остаток: только формализация)

**Статус основной части (выполнено 28.08.2026):** grounded-контур реализован — retrieval/prompt тюнинг (реестр 13 проектов в промпте, промпт `v4-compact-multi`), registry-only cache policy, устранены provenance-дефекты, кросс-сессионная контаминация кеша и дрейф памяти. Production eval (baseline + 5 re-runs): корректность **159/168 = 94,6%** и **p95 4956 мс < 5 сек** — приняты владельцем; C-класс 68/77 = 88,3% принят без re-run #6 (протокол — `task_history/2026-08-28_task-aip-diagnostic-eval.md`).

**Остаток §4.3 — отдельная небольшая задача: формализация результатов**

| # | Работа | Результат |
|---|--------|-----------|
| 3.5 | `docs/AI_EVAL_REPORT.md` — сводный отчёт приёмки (методика, принятые метрики, известные ограничения) | Отчёт в репозитории |
| 3.6 | Обезличенный regression set — небольшой безопасный набор контрольных вопросов без traces/production-данных | Файл в репозитории |

**Разделение артефактов:**

- **В публичный репозиторий допускается** только небольшой обезличенный regression set (контрольные вопросы/ожидания без production-данных).
- **Полный диагностический benchmark, traces, production snapshots и adjudication остаются в private persistent volume** (`ai-portfolio-data:/eval/`, манифест 50 файлов) — публикация полного диагностического набора не планируется.

### 4.4 Morphic — PLANNED / OWNER DECISION GATE, отдельный поток P3 (объём фиксируется после утверждения UX-контракта)

Статус: **PLANNED / OWNER DECISION GATE**. Morphic — согласованное направление следующего этапа и **возможный способ реализации обязательного требования M4**, но не новое обязательство утверждённого ТЗ. Точный UX-контракт ещё не утверждён. До утверждения UX-контракта Morphic **не блокирует** eval (§4.3), обязательный E2E (§4.6) и release; обязательный presale-путь должен работать и без Morphic — через существующую главную и AI-ассистента. Перенос Morphic не является сокращением утверждённого Must Have и не оформляется как DEFER CANDIDATE.

Morphic применяется на уровне поиска и выбора релевантного кейса; главная страница — сцена взаимодействия с портфелем; внутри страницы кейса действует фиксированный Narrative. Morphic не переносится внутрь лендинга и не заменяет его структуру; Morphic не был частью выполненной пилотной интеграции главной. Новые сервисы, сущности, Generative UI и архитектурные компоненты добавляются только по отдельному решению владельца.

| ID | Работа | Зависимость | Результат |
|----|--------|-------------|-----------|
| MORPHIC-1 | Спроектировать UX-контракт Morphic-сцены (отдельная задача на утверждение владельцу) | — | Утверждённый UX-контракт |
| MORPHIC-2 | Определить взаимодействие Morphic-сцены с существующей главной и AI-ассистентом | MORPHIC-1 | Согласованная схема взаимодействия |
| MORPHIC-3 | Реализовать поиск и представление релевантных кейсов с объяснением применимости (после утверждения контракта) | MORPHIC-2 | Работающая Morphic-сцена |
| MORPHIC-4 | Сохранить прямой переход на существующие лендинги кейсов | MORPHIC-3 | Переходы `/cases/...` работают, Narrative не затронут |
| MORPHIC-5 | Проверить основной presale-путь через Morphic-сцену: задача → релевантный кейс → доказательства → обращение | MORPHIC-4 | Путь проходим через Morphic |
| MORPHIC-6 | Дополнительные E2E-сценарии Morphic — только после включения Morphic владельцем в release scope | MORPHIC-5 | E2E-кейсы Morphic в наборе тестирования |

Все зависимости ссылаются только на предшествующие задачи. Числовая оценка реализации Morphic (MORPHIC-3…6) не входит в суммарный объём §5 до утверждения UX-контракта.

### 4.5 Presale-аналитика — P4, Must, 1.5–2 дня

| # | Работа | Результат |
|---|--------|-----------|
| 5.1 | Модель данных presale-событий (events/funnel) и endpoint'ы | События фиксируются |
| 5.2 | Инструментация ключевых точек пути: посещение → просмотр кейса → взаимодействие с ассистентом → обращение | Presale-путь измерим |
| 5.3 | Базовое представление воронки в Admin Console | Воронка видна администратору |

### 4.5а Admin Console: UI управления допуском источников KB — P3, Must, 0.5–1 день

Выявленный разрыв (28.08.2026): требование M6 (TZ) — «Admin Console управляет источниками и составом индексируемых материалов». Backend готов полностью (PATCH `admission_status`/`include_patterns`/`exclude_patterns`, endpoint `admission-preview` — вся кампания допуска 12 источников прошла через API), но в `admin/src/` UI управления допуском отсутствует. Без этого пункта E2E административных сценариев (§4.6, п.6.3) тестировал бы функции, отсутствующие в UI.

| # | Работа | Результат |
|---|--------|-----------|
| 5а.1 | Отображение `admission_status` и паттернов в списке источников KB | Состояние допуска видна администратору |
| 5а.2 | Редактирование `include_patterns` / `exclude_patterns` и статуса допуска через UI | Управление составом индексации без API |
| 5а.3 | Просмотр admission-preview (состав файлов к индексации) перед sync | Контроль перед индексацией |

Выполняется до §4.6 (тестирование); рационально — в один проход с §4.5.

### 4.6 Тестирование — P5, Must, 1.5–2 дня

| # | Работа | Результат |
|---|--------|-----------|
| 6.1 | Функциональные тесты публичной витрины и Admin Console | Отчёт |
| 6.2 | E2E пользовательских сценариев, включая обязательный presale-путь через существующую главную и AI-ассистента; Morphic-сценарии включаются только если владелец включил Morphic в release scope (MORPHIC-6) | E2E PASS |
| 6.3 | E2E административных сценариев (проекты, источники KB, синхронизация) | E2E PASS |
| 6.4 | Проверка ссылок (GitHub/demo/landing) | 0 битых ссылок |
| 6.5 | AI eval подтверждение на production-конфигурации | ≥90% / <5 сек |
| 6.6 | Security checks; graceful degradation (недоступность AI не блокирует витрину) | Проверено |
| 6.7 | Итог: 0 критических дефектов | Отчёт тестирования |

### 4.7 Deployment Validation и публикация — P5/P6, Must, 2–3 дня

| # | Работа | Результат |
|---|--------|-----------|
| 7.0 | **Блокер перед §7.1:** `docs/DEPLOYMENT_GUIDE.md` в репозитории отсутствует (ссылки на него в `PROJECT_STATE.md`/`SPEC.md` — мёртвые). По правилам APL DEPLOYMENT_GUIDE — Source of Truth воспроизводимости; до его создания или восстановления Deployment Validation невозможна. Руководство создаётся в рамках §4.7, до начала §7.1 | DEPLOYMENT_GUIDE существует и актуален |
| 7.1 | Deployment Validation AI Portfolio в чистом окружении по `docs/DEPLOYMENT_GUIDE.md` + отчёт | Отчёт PASS |
| 7.2 | Ревизия `.gitignore` и поставки: orphaned-файлы, уже отслеживаемые Git, не исключаются самим `.gitignore` — для каждого файла применяется одно из решений владельца: удалить; перенести за пределы публичной сборки; явно исключить из процесса сборки/публикации | Чистая публичная поставка |
| 7.3 | Ревизия и финализация публичного репозитория `AlexLvGulyaev/AI-Portfolio` (репозиторий уже опубликован и содержит проект) | Поставка актуальна и чиста |
| 7.4 | Production release на VPS: миграции, backup, health checks, финальный E2E | Release 04.09.2026 |

---

## 5. Календарная раскладка (28.08 → 04.09.2026, только фактический остаток)

Выполненные 28.08 работы (§4.1 admission gate, §4.2 sync/reindex 12 источников, §4.3 retrieval/prompt + основная eval-кампания и её формализация) **не перепланируются** — в раскладке только незавершённые работы.

**Критический Must-путь (последовательно):**

| Окно | Работы |
|------|--------|
| 28–29.08 | §4.5а UI управления допуском источников KB (0.5–1 день); решение владельца по orphaned-файлам (п.1.6) |
| 29–31.08 | Финализация документации AI Portfolio (работа владельца, параллельно с агентскими потоками) |
| 31.08–01.09 | §4.6 тестирование: функциональные, E2E presale-путь, administrative E2E |
| 01–03.09 | §4.6 E2E + security; перед Deployment Validation — собственный корпус AIP (цикл «допуск → sync → reindex → контрольный eval», отложен владельцем на зрелую документацию) |
| 01–03.09 | §4.7 п.7.0: создание/восстановление `docs/DEPLOYMENT_GUIDE.md` (блокер §7.1) |
| 03–04.09 | §4.7 Deployment Validation → публикация + production release |

**Параллельные работы (не блокируют критический путь до §4.6):**

| Окно | Работы |
|------|--------|
| 28–30.08 | §4.5 presale-события и воронка в Admin Console (1.5–2 дня) |

**Неблокирующий OWNER DECISION GATE:**

| Окно | Работы |
|------|--------|
| по решению владельца | Morphic (§4.4): проектирование UX-контракта (MORPHIC-1) — не блокирует ни один шаг критического пути; реализация MORPHIC-2…6 — только после утверждения UX-контракта и решения о включении в release scope |

**Оценка объёма (без Morphic, только незавершённые работы):** остаточная сумма составляет **6.5–9.5 рабочих дней** (остаток §4.3 формализация: 0.25–0.5; 4.5а: 0.5–1; 4.5: 1.5–2; 4.6: 1.5–2; 4.7: 2–3; цикл собственного корпуса — допуск → sync → reindex → контрольный eval: 0.5–1). В оценку **не включены** выполненные 28.08: admission gate (§4.1), sync/reindex 12 источников (§4.2), основная eval-кампания и тюнинг retrieval/prompt (§4.3). Критический путь остатка — 4.5а → 4.6 → 4.7 (плюс собственный корпус перед 4.7) — **4.5–7 рабочих дней**; presale-аналитика (§4.5) и MORPHIC-1 — независимые параллельные работы. Числовая оценка реализации Morphic (MORPHIC-3…6) не включена до утверждения UX-контракта. Финализация документации AI Portfolio (шаг 7 остаточного пути) — работа владельца, в агентскую оценку не входит.

**Риск сроку (по фактическому остаточному объёму):** от 28.08 до 04.09.2026 — 7 календарных дней; остаточные 6.5–9.5 рабочих дней укладываются только при работе без простоев (включая выходные) и параллелизации §4.5 с §4.5а/§4.6. Риск — средне-высокий, сохраняющий силу. Если объём реально не укладывается, сокращение scope возможно **только** через DEFER CANDIDATE на решение владельца — незавершённый Must Have из плана не удаляется и календарный срок не переносится самовольно.

---

## 6. DEFER CANDIDATE

**На текущий момент DEFER CANDIDATE не утверждаются.** Решение о любом переносе принимает только владелец.

Кандидаты, которые могут быть оформлены **только по явному решению владельца** при угрозе сроку 04.09.2026 (перечисление не является утверждением):

- ML-контур HR Assistant / HRA-LoRA (не на критическом пути AI Portfolio);
- расширенная визуальная presale-аналитика (Should/Could);
- сохранение истории диалога (Should, post-MVP).

Morphic не включается в DEFER CANDIDATE: перенос Morphic не является переносом утверждённого Must Have (статус PLANNED / OWNER DECISION GATE, см. §4.4); presale-путь через существующую главную и AI-ассистента сохраняется независимо.

Если в ходе реализации работа угрожает сроку, применяется последовательность: перепланирование → параллелизация → проверка критического пути → и только затем оформление DEFER CANDIDATE с указанием проекта/функции, причины, влияния на результат и критерии приёмки, оценки выигрыша по сроку.

---

## 7. MoSCoW mapping (остаточные работы)

| Требование | MoSCoW | Статус |
|------------|--------|--------------|
| Публичная витрина (13 полноценных кейсов) | Must | COMPLETED |
| KB admission gate | Must | COMPLETED — 28.08 |
| GitHub Sync 12 репозиториев + ChromaDB reindex | Must | COMPLETED — 12 / 211 / 5253 |
| AI-ассистент: grounded-ответы, ≥90%, <5 сек | Must | COMPLETED / OWNER ACCEPTED — 94,6%, p95 4956 мс |
| Runtime fallback генерации и retrieval | Needs Verification | Известный принятый риск; реализация отложена |
| Поиск релевантных кейсов, объяснение применимости, прямые переходы (M4) | Must | NEEDS VERIFICATION — формализация eval (3.5–3.6) завершена, но механическое выполнение M4 (поиск, объяснение применимости, переходы на лендинги) по факту не доказано; проверка — в E2E §4.6 (обязательный presale-путь) |
| Morphic-сцена поиска и выбора кейса | PLANNED / OWNER DECISION GATE (возможный способ реализации M4; объём после утверждения UX-контракта) | 4.4 |
| Измерение presale-пути + базовая аналитика | Must | 4.5 |
| Admin Console: UI управления допуском источников | Must (закрытие M6) | 4.5а |
| Тестирование и E2E, 0 критических дефектов | Must | 4.6 |
| Deployment Validation | Must (критерий готовности публичного проекта) | 4.7 |
| Автоматическая синхронизация KB по расписанию | Should | post-release |
| Сохранение истории диалога | Should | post-MVP |
| Расширенная аналитика; резюме диалога | Could | post-MVP |

---

## 8. Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Полный остаточный объём не укладывается до 04.09.2026 | Средняя–высокая | Высокое | Параллелизация потоков (§5); Must Have приоритет; DEFER CANDIDATE только по решению владельца |
| Допуск собственного корпуса AIP перед Deployment Validation: документация AI Portfolio ещё финализируется — индексация «летящего» корпуса даст устаревший индекс и потребует повторного цикла «допуск → sync → reindex → eval» | Средняя | Среднее | Цикл выполняется один раз на зрелой документации (решение владельца 28.08); состав допуска — через admission gate (include/exclude-паттерны); контрольный eval вместо полной кампании |
| C-класс корректности стохастически колеблется между прогонами (принятый итог overall — 94,6%, C-класс 88,3%) | Средняя | Среднее | Принято владельцем (28.08.2026); повторные прогоны — только по согласованию; точечные улучшения retrieval/генерации — отдельные согласованные задачи |
| Запас production p95 до 5 секунд минимален (4956 мс, единичные выбросы) | Средняя | Среднее | Мониторинг latency; при деградации — декомпозиция по трейсам |
| LLM-ответы временно не кешируются (registry-only политика) | — | Низкое | Осознанный компромисс после устранения контаминации кеша; кеширование вернётся с признаком cache-eligible |
| Retrieval зависит от OpenAI embeddings; runtime fallback retrieval не реализован и отложен решением владельца | Низкая–средняя | Среднее | GigaChat-fallback покрывает только генерацию; варианты решения — в `task_history/2026-08-28_task-aip-diagnostic-eval.md` |
| DEPLOYMENT_GUIDE отсутствует; после создания (§4.7 п.7.0) Validation выявляет его дефекты | Средняя | Высокое | Создание/восстановление руководства — явный шаг п.7.0 до §7.1; резерв времени 01–03.09; правки документации до завершения validation по правилам APL |
| ChromaDB reindex медленный | Средняя | Среднее | Ночной запуск; инкрементальная синхронизация |
| Morphic-контракт не утверждён вовремя | Средняя | Среднее | Morphic реализуется после eval; прямой presale-путь через главную сохраняется независимо |

---

## 9. Критерии готовности остаточных этапов

| Этап | Критерий |
|------|----------|
| 4.1 | По каждому источнику: определён репозиторий, проверено качество, определён состав индексации, исключения настроены |
| 4.2 | Все допущенные источники подключены; ChromaDB пересобрана; в индексе нет секретов/внутренних материалов |
| 4.3 | ~~Eval ≥90%, latency <5 сек, fallback работает, ответы grounded с источниками~~ **COMPLETED 28.08:** eval 94,6% (≥90%), p95 4956 мс (<5 сек), ответы grounded с источниками — принято владельцем. Runtime fallback **не входит** в критерий: это отдельный известный принятый риск (§2 NEEDS VERIFICATION, §8) |
| 4.4 | Только при включении Morphic в release scope: UX-контракт утверждён владельцем; Morphic-сцена работает; переход на лендинги прямой; Narrative страниц кейсов не изменён. Без Morphic критерий неприменим — обязательный presale-путь работает через §4.3/4.6 |
| 4.5 | Presale-события фиксируются; воронка видна в Admin Console |
| 4.6 | Все тесты и E2E PASS (обязательный presale-путь; Morphic-сценарии — только при включении Morphic в release scope); 0 критических дефектов |
| 4.7 | Deployment Validation PASS в чистом окружении; репозиторий опубликован; production release 04.09.2026 |

---

## 10. Артефакты остаточных работ

| Артефакт | Расположение |
|----------|--------------|
| Отчёт KB admission gate по источникам | `docs/` кейса |
| `docs/AI_EVAL_REPORT.md` | Результаты eval AI-ассистента |
| `docs/DEPLOYMENT_VALIDATION_REPORT.md` | Отчёт Deployment Validation |
| UX-контракт Morphic-сцены | Отдельная задача (`task_history/`) |
| `backend/tests/ai_portfolio_eval_set.json` | Контрольный набор вопросов |

---

## 11. История изменений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 2026-07-12 | 1.0 | Базовый IMPLEMENTATION_PLAN для первой версии AI Portfolio |
| 2026-08-18 | 2.0 | 10-дневный план финализации AI Portfolio |
| 2026-08-18 | 3.0 (черновик) | 17-дневный план 19.08–04.09.2026 с разовой финализацией 12 проектов |
| 2026-08-18 | 3.1 | Полная декомпозиция EXISTING DEBT + AIP INTEGRATION + NEW GAP по 12 проектам; параллельные потоки; DEFER CANDIDATE только по решению владельца; статус «Утверждено» |
| 2026-08-25 | 3.2 | Корректировка после интеграции dual-theme главной страницы: 13 управляемых карточек (12 проектов + Prompt Review placeholder), актуализация ссылок на TZ v1.4 / SPEC v2.1 |
| 2026-08-25 | 3.3 | Зафиксирован принятый шаблон лендинга кейса (`AIP Case Landing Page Standard v1.1`, эталон — AI Curator) |
| 2026-08-27 | 4.0 | Перестроен как остаточный план от состояния на 27.08.2026: выполненные работы 19–26.08 перенесены в компактный блок COMPLETED; введены статусные разделы COMPLETED / IN PROGRESS / NOT STARTED / BLOCKED / NEEDS VERIFICATION; восстановлена критическая зависимость «готовность документации → допуск источников (KB admission gate) → GitHub Sync → ChromaDB reindex → настройка retrieval/prompt → eval → E2E → release»; целевой состав GitHub Sync — 12 проектных репозиториев + собственный корпус AI Portfolio (13 полноценных кейсов; Prompt Review — finished-case; публичное существование репозиториев `Retail-Group` и `AI-Portfolio` проверено по живому состоянию GitHub); добавлены задачи Morphic (UX-контракт → взаимодействие с главной и AI-ассистентом → поиск и представление кейсов → прямые переходы на лендинги → presale-путь → E2E) с границей: Morphic не реализован, не заменяет Narrative лендингов; зафиксирован риск сроку и перечень DEFER-кандидатов, утверждаемых только владельцем. Срок 04.09.2026 и метрики ≥90% / <5 сек сохранены. **Корректирующая сверка 27.08.2026** (без повышения версии): Morphic переведён в статус PLANNED / OWNER DECISION GATE с нумерацией MORPHIC-1…6 и зависимостями только на предшественников; исключён из DEFER CANDIDATE и из числовой оценки объёма; обязательный E2E ограничен presale-путём; уточнено решение по orphaned-файлам и роли `.gitignore`; оценка объёма пересчитана (9–13 рабочих дней суммарно; критический путь 8–11 рабочих дней) |
| 2026-08-28 | 4.1 | **Корректирующая сверка 28.08.2026.** Реализован KB admission gate (fail-closed, миграция 014, guard'ы sync/delete, тесты; коммит `f899f24`); KB переведена на collection `ai_portfolio_knowledge_v2` (12 источников / 211 документов / 5253 чанка), старая collection удалена; проведена диагностическая eval-кампания production-ассистента (baseline + 5 re-runs): provenance-дефекты, кросс-сессионная контаминация кеша и дрейф памяти устранены (registry-only cache policy, KEY_SCHEMA `v4-registry-only`; промпт `v4-compact-multi` с детерминированным реестром 13 проектов). Итог eval: 159/168 = 94,6% формально; C-класс 68/77 = 88,3% после adjudication — принят владельцем (вариант «а»); latency p95 4956 мс < 5000 мс — критерий выполнен. Eval-данные удалены из production БД с разрешения владельца. Из §2 статусов убраны: «KB admission gate» (NOT STARTED → COMPLETED), блокировка eval; в NEEDS VERIFICATION зафиксирован риск отсутствия fallback-канала retrieval (решение владельца — пока не реализовывать). Выявлен разрыв M6: UI управления допуском источников в Admin Console отсутствует (backend готов, кампания допуска прошла через API) — добавлена задача §4.5а (0.5–1 день, до §4.6); оценка объёма пересчитана (9.5–14; критический путь 8.5–12) |
| 2026-08-28 | 4.3 | **Механическая синхронизация статусов с закрытой eval-задачей.** §4.1, §4.2 и основная часть §4.3 переведены в COMPLETED (12 / 211 / 5253 unique ID; 94,6% / p95 4956 мс — OWNER ACCEPTED); из MoSCoW убраны будущие ссылки 4.1/4.2/4.3, fallback генерации/retrieval вынесен отдельной строкой Needs Verification; остаток §4.3 — только формализация (`AI_EVAL_REPORT.md`, обезличенный regression set; полный диагностический набор остаётся в private volume, публикация не планируется); критический путь переписан как остаточный (10 шагов, §4.1–4.3 выведены); оценка пересчитана только по незавершённым работам — 6.5–9.5 рабочих дней, критический путь остатка 4.5–7; риски «eval <90%» заменены фактическими (стохастичность C-класса, минимальный запас p95, LLM-ответы не кешируются, зависимость retrieval от OpenAI embeddings, отложенный fallback retrieval) |
| 2026-08-28 | 4.2 | **Решение владельца по собственному корпусу.** Подключение собственного корпуса AI Portfolio (13-й источник, §4.2) отложено до финализации доработки AIP и документации проекта; подключение — непосредственно перед Deployment Validation, цикл «допуск → sync → reindex → eval» — один раз на зрелой документации. §4.2 в части 12 проектных репозиториев закрыт (28.08); остаток §4.2 снимается с активной очереди |
| 2026-08-28 | 4.4 | **Финальная механическая сверка трёх документов (28.08, вечер).** `AI_EVAL_REPORT.md`: методика уточнена (функциональный benchmark — in-process на production-компонентах, публичный HTTP — smoke/latency; класс L — изолированные fixtures); класс H исправлен на 8/8; диапазон C-класса — 88,3–90,9% (не 88–96%); для regression set зафиксировано: JSON-валидация пройдена, набор ещё не запускался; runner исправлен (default `AIP_BASE_URL=http://localhost:8000`, production только через явную env; уникальный `visitor_id` на запуск; независимые `session_id` для тестов без `session_group`; `raise_for_status()`). План: шапка на состояние 28.08 (v4.4); §1.3 — из остатка убраны KB/admission/retrieval-prompt/eval; §4.0 разделён на критический Must-путь / параллельные работы / Morphic как неблокирующий OWNER DECISION GATE; календарь переписан только на фактический остаток до 04.09 без перепланирования §4.1–4.3; MoSCoW M4 отвязан от завершённой формализации (NEEDS VERIFICATION до E2E §4.6); критерий §4.3 — без «fallback работает» (отдельный принятый риск); риск документации 12 источников заменён риском допуска собственного корпуса AIP; отсутствие `docs/DEPLOYMENT_GUIDE.md` зафиксировано как блокер п.7.0 перед Deployment Validation (§4.7) |