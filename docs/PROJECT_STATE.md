# 📊 PROJECT_STATE.md — AI Portfolio

**Проект:** ai-portfolio
**Дата создания:** 2026-07-12
**Последнее обновление:** 2026-09-03
**Статус:** Публичная поверхность AI Portfolio в основном завершена. Портфель состоит из **13 полноценных проектов** — Prompt Review завершён как полноценный кейс (finished-case) с публичным репозиторием, для всех 13 проектов существуют полноценные страницы кейсов по стандарту `AIP Case Landing Page Standard v1.1`; фактический эталон шаблона — рабочая страница AI Curator (`/cases/ai-curator.html`, а не устаревший `ai-curator-template-preview.html`). Главная страница (`/`) одновременно является витриной всех проектов; устаревший каталог с 12 тёмными карточками (`portfolio.html`) и маршрут `/projects` переадресуют на `/`. Страницы «Услуги» и «Контакты» созданы и включены в общую навигацию. Production URL `https://ai.alex-n8n.site/` работает. **KB-контур ядра завершён (28.08.2026):** все 12 репозиториев видимой витрины допущены через KB admission gate в порядке `display_order` и синхронизированы в `ai_portfolio_knowledge_v2` (12 источников, 211 документов, 5253 чанков); production AI-ассистент переключён на `v2`, старая collection `ai_portfolio_knowledge` удалена (28.08.2026, после завершения кампании). Дальнейшая работа сосредоточена на ядре: **eval-кампания AI-ассистента закрыта (28.08.2026, результат принят владельцем)** — итоговая корректность **159/168 = 94,6%**, production **p95 4956 мс** (критерий <5 секунд выполнен); C-класс 88,3% принят владельцем без re-run #6. Остаток ядра: Morphic-проба реализована (03.09.2026 — подбор кейса по задаче зрителя, `POST /case-match`, панель документа в ассистенте; полный UX-контур — отдельное направление), тестирование и production release к 04.09.2026 (Deployment Validation выполнена 02.09.2026, PASS — dind-стенд, 3 прогона fresh-from-zero, гайд v1.1; см. §2) (**UI управления допуском в Admin Console — Must-задача §4.5а — выполнена 28.08.2026**: Admission Console на `/content/sources`, draft/effective-модель правил, immutable-превью с commit SHA, stale-защита одобрения, двойная защита прямого PATCH, мутационные сценарии 23/23 PASS; production-состав 12 источников не изменён). Известное ограничение качества источников: не у всех портфельных проектов документация доведена до стандартов APL — оно не блокирует завершённую кампанию допуска и учитывается составом индексации.

---

## 🧾 1. Project Summary

AI Portfolio — персональный публичный сайт AI-инженера с интегрированным AI-ассистентом. Платформа объединяет **13 полноценных реализованных AI-решений** в единую витрину и сокращает путь потенциального заказчика до релевантного кейса, доказательств реализации и обращения к исполнителю.

**Утверждённый состав публичной витрины:** 13 полноценных проектов — включая **Prompt Review**, завершённый как полноценный кейс (finished-case) с публичным репозиторием `AlexLvGulyaev/PromptReview`. Placeholder-статус Prompt Review отменён.

**Ключевые параметры:**

| Параметр | Значение |
|----------|----------|
| Frontend | Vanilla HTML/CSS/JS (`src/`) |
| Backend | FastAPI + PostgreSQL + ChromaDB HTTP (`backend/`) |
| Admin Console | React + TypeScript + Vite (`admin/`) |
| LLM | OpenAI active + GigaChat fallback |
| Embeddings | OpenAI `text-embedding-3-small` |
| Vector store | ChromaDB HTTP server (`ai-portfolio-chroma`) |
| Auth | Bearer token (`ADMIN_API_TOKEN`) |
| Public URL | `https://ai.alex-n8n.site` |

---

## 🔄 2. Current Status

**Стадия:** Подготовка к production-ready release согласно `docs/TZ.md` (утверждено) и остаточному `docs/IMPLEMENTATION_PLAN.md` от 27.08.2026. Целевая дата production-ready release: **04.09.2026**.

**Фактически реализовано — публичная поверхность (в основном завершена):**
- **Главная страница** (`src/index.html`): dual-theme, динамическая загрузка карточек через `/project-cards`, featured/archive-логика (первые три карточки по `display_order` — флагманы, остальные — архив), переключатель темы, чат-виджет. Главная страница одновременно является витриной всех проектов: пункт навигации «Проекты» ведёт на `/`.
- **13 полноценных лендингов кейсов** в `src/cases/` соответствуют стандарту `AIP Case Landing Page Standard v1.1` (hero с outcome-first, summary grid, тройка «Результат / Проблема / Решение», metrics grid, pipeline SVG, scenario SVG, quote, final CTA). Фактический эталон шаблона — рабочая страница AI Curator (`/cases/ai-curator.html`); файл `ai-curator-template-preview.html` является устаревшим backup-шаблоном и не является эталоном. AI Curator — ручная страница (`MANUAL_CASES` генератора, не перезаписывается).
- **Prompt Review — полноценный finished-case** с публичным репозиторием (`AlexLvGulyaev/PromptReview`) и документацией; placeholder-статус отменён.
- **Страницы «Услуги» (`services.html`) и «Контакты» (`contacts.html`)** созданы и включены в общую навигацию.
- **Устаревший каталог** `portfolio.html` (старая страница с 12 тёмными карточками) и маршрут `/projects` переадресуют на `/`; устаревшая страница не является текущей основной витриной. Orphaned-файлы (`competitor-monitor.html`, `lead-qualification-nb.html`, `telegram-ai-gateway.html`, `ai-curator-template-preview.html`) перенесены владельцем в архив legacy-черновиков вне публичной сборки (решение 02.09.2026, коммит `f54c776`); из production-выдачи исчезнут при следующем ребилде `ai-portfolio-frontend` (правки запечены в образ).
- Генератор `build_registry.py` + `generate_landings.py` работает, повторная генерация детерминированна.

**Фактически реализовано — инфраструктура и backend:**
- FastAPI + PostgreSQL + ChromaDB HTTP развёрнуты на VPS; production URL `https://ai.alex-n8n.site`.
- `ProjectCard` в PostgreSQL — SOT карточек проектов; Deprecation `show_on_homepage` (используется только `is_visible` + `display_order`).
- Admin Console v1 (React + TS + Vite), 5 разделов: Dashboard, Content/KB, Logs, Conversations, Audit.
- Multi-provider LLM: OpenAI active + GigaChat fallback; execution tracing, operational logs, аудит входа в админку и посещений сайта.

**Фактически реализовано — AI-ассистент (KB-контур завершён):**
- Chat-widget на всех публичных страницах; endpoint `POST /chat` с указанием источников.
- **Производственная пересборка KB (28.08.2026):** развёрнут KB admission gate (миграция `013 → 014`), старый KB-контур PostgreSQL очищен (4 таблицы KB).
- **Кампании диалогового допуска завершены (28.08.2026):** все **12 репозиториев видимой витрины** допущены через admission gate в порядке `display_order` (диалоговая регистрация → admission-preview → чтение кандидатов → регистр INCLUDE/EXCLUDE → решение владельца → PATCH → sync) и синхронизированы в `ai_portfolio_knowledge_v2`: **12 источников, 211 документов, 5253 чанков, дубликатов 0**; исключённые материалы (паспорта проектов, планы, тестовые каталоги, внутренние шаблоны, документ с секретом) в индекс не попали. Старая collection `ai_portfolio_knowledge` (5400 чанков) после завершения кампании удалена (28.08.2026).
- **Ассистент переключён на `v2` (28.08.2026):** `CHROMA_COLLECTION_NAME=ai_portfolio_knowledge_v2` в compose, backend пересоздан, функциональная проверка на контенте, существующем только в v2 — пройдена. Откат — удаление одной строки compose + recreate.

**Реализовано 03.09.2026 (день релизной полировки):**
- **Morphic-проба (`POST /case-match`):** подбор до 3 релевантных кейсов под задачу зрителя на главной странице (grounded — retrieval по документам KB + реестр видимых карточек; «why»-объяснение генерирует LLM по документам, без обещаний статусов и цен; scope пробы — без кеша и сессий, откат — флаг `MORPHIC_ENABLED` на фронте + revert). Верифицирована Playwright в проде (6/6 PASS).
- **Панель документа в ассистенте:** чип источника с repo+path+excerpt — кнопка; `GET /document-fragment` возвращает фрагмент документа вокруг цитированного чанка с подсветкой (ветка fail-closed из реестра допуска; документ приводится тем же plain-трансформом, что и при синхронизации KB — чанк локализуется по построению; честный фолбэк при правке документа после индексации).
- **Фикс статуса «Есть изменения» Admission Console:** `has_draft_changes` переведён на сравнение значений черновика с эффективными правилами (было — по факту наличия draft-колонок); «Сохранить» активна и при отличии состава от одобренного (UI); hero-область кейсов расширена 640→780px (подсказка демо больше не сдвигает скриншоты).

**Что не реализовано (ядро, оставшиеся работы до 04.09.2026):**
- Формализация eval-артефактов (`docs/AI_EVAL_REPORT.md`, контрольный eval set в репозитории) — сама eval-кампания выполнена 28.08.2026, результат принят владельцем.
- **Morphic (полный UX-контур)** — проба реализована 03.09.2026 (подбор кейса по задаче зрителя, `POST /case-match`); полный контур (сессии, визард, сцена) — отдельное направление следующего этапа, UX-механика подлежит проектированию.
- ~~Presale-события и presale-аналитика в Admin Console — не добавлены~~ **Реализовано 02.09.2026:** консоль «Аналитика → Пресейл» (§4.17–4.18 плана): события воронки (`/track-visit`, `/track-event`), аналитический дашборд с провалом вглубь (уровень 2 «посетители шага», уровень 3 «путь гостя», KPI-дельты).
- ~~**Admin Console: UI управления допуском источников KB — отсутствует**~~ **Реализовано 28.08.2026 (§4.5а закрыт):** Admission Console на `/content/sources` — требование M6 закрыто полностью: draft/effective-модель правил (effective меняется только одобрением immutable-превью), state machine допуска, stale-защита одобрения (patterns/commit), блокировка/разблокировка с аудитом событий, двойная защита прямого PATCH (409 `use_admission_actions`). Мутационные сценарии 23/23 PASS, unit-тесты гвардов 23/23; production-состав 12 источников не изменён.
- ~~Deployment Validation — не начата~~ **Выполнена 02.09.2026, PASS (обязательный критерий готовности публичного GitHub-проекта по правилам APL закрыт):** `docs/DEPLOYMENT_GUIDE.md` создан 02.09 (v1.0, коммит `f919155`) и валидирован запуском — dind-стенд `aip-dv-dind` (чистый Docker Host), свежий клон публичного GitHub, секреты перенесены программно. 3 прогона fresh-from-zero: №1 FAIL (дефект цепочки миграций — сид 003 зависел от колонки из 004; фикс `c00bb6a`), №2 FAIL (013 прятал канонический prompt-review; фикс `48f8751`), №3 **PASS** — 5/5 контейнеров healthy, 22/22 миграций, 13 видимых карточек = прод, KB bootstrap через Admin API (5 документов / 113 чанков, состав = прод), живой чат с источниками за 2,9 с. Гайд актуализирован до v1.1 (`398163a`). Протокол — в документации проекта.
- Публичный репозиторий `AlexLvGulyaev/AI-Portfolio` уже опубликован и содержит проект (src, backend, admin, docs, docker-compose); остаточная работа — ревизия поставки (`.gitignore`, внутренние артефакты) и актуализация документации через admission gate, а не первичная публикация.

**Состав GitHub Sync (реестр проверен независимо от лендингов, 27.08.2026; кампания допуска завершена 28.08.2026):**

Количество кейсов, репозиториев и логических KB-источников — разные числа; их не следует смешивать. Проверка выполнена по локальным git-remotes проектных каталогов и по GitHub API (`private: false`, наличие контента); ссылки на страницах кейсов основанием не являются.

- 11 кейсов имеют локальные Git-репозитории с origin на GitHub: `AI-Curator`, `ai-data-assistant`, `Assistant-Flow`, `HR-Assistant`, `meeting-audit-bot`, `Lead-Qualification-MVP`, `PromptReview`, `review-auto-responder`, `Review-Flow`, `telegram-intake-bot`, `telegram-onboarding-bot` — статус `VERIFIED_REPOSITORY`.
- **HR-Assistant** — статус `SHARED_REPOSITORY`: обслуживает два кейса (HR Assistant и HRA-LoRA; LoRA-контур — `finetuning/` внутри того же репозитория, проверено локально).
- **Retail-Group** — публичный репозиторий существует (GitHub API: README + `docs/` с пресейл-материалами); локальный каталог является Git-репозиторием (уточнено 28.08.2026, HEAD == remote); допущен через admission gate — статус источника `ADMITTED` (см. §2.2).
- Собственный корпус — `AI-Portfolio` (локальный git-remote + публичный репозиторий с контентом проекта) — `VERIFIED_REPOSITORY`.

**Разделение текущей и будущей KB (однозначно):**

- **Текущая production KB — 12 репозиториев видимой витрины** (12 источников / 211 документов / 5253 чанка / 5253 unique ID в `ai_portfolio_knowledge_v2`): кампания допуска завершена 28.08.2026, состав подтверждён и утверждён владельцем (§2.2). Дополнительных подтверждений не требует.
- **Будущий отдельный допуск — собственный корпус AI Portfolio (13-й источник):** отложен решением владельца (28.08.2026) до финализации документации проекта; подключение перед Deployment Validation, цикл «допуск → sync → reindex → один контрольный eval» — один раз на зрелой документации (§2.2).

Полная таблица «кейс → репозиторий → основание → статус» — в [`docs/PORTFOLIO_OVERVIEW.md`](PORTFOLIO_OVERVIEW.md) (карта портфеля) и на страницах кейсов.

**2.1. Качество проектной документации и KB admission gate**

Зафиксированное ограничение (27.08.2026):

- Не у всех портфельных проектов документация доведена до стандартов APL.
- Подключение репозитория к GitHub Sync **не означает** индексацию всех найденных в нём Markdown-файлов.
- Загрузка в KB устаревшей, противоречивой, внутренней или фактически неточной документации недопустима: качество источников напрямую определяет достижение обязательной метрики корректности AI-ассистента **≥90%**.

Для каждого источника вводится **KB admission gate** — реализация уже утверждённого требования M6 (управление источниками и составом индексируемых материалов), а не новая архитектура:

1. Определить фактический репозиторий источника.
2. Проверить публичность, актуальность и непротиворечивость документов.
3. Определить разрешённый состав индексируемых материалов.
4. Исключить из индексации внутренние, устаревшие и неподтверждённые документы.
5. При отсутствии пригодного корпуса — сначала актуализировать минимальный канонический публичный документ проекта.
6. Только после допуска подключать источник и выполнять reindex.
7. После формирования чистого корпуса — запускать eval и настройку retrieval/prompt.

**Статус (28.08.2026):** кампания допуска завершена — все 12 репозиториев витрины допущены и синхронизированы; незавершённость документации отдельных проектов **не является блокировкой** уже завершённой кампании допуска. Она сохраняется как **известное ограничение качества источников**: при будущих допусках (включая собственный корпус AI Portfolio) применяется тот же механизм — admission gate с выборочным составом индексации и, при необходимости, актуализацией минимального канонического документа. Исправление документации всех портфельных проектов не входит в текущий проход.

**Готовность механизма:** backend admission gate готов полностью (миграция 014, PATCH `admission_status`/include/exclude, `admission-preview`, guard'ы sync/delete, тесты — коммит `f899f24`); **UI управления допуском реализован 28.08.2026 (§4.5а)** — Admission Console на `/content/sources`, миграция 015 (kb_admission_previews, kb_admission_events), consistent draft/effective-модель, stale-защита одобрения, аудит решений.

**2.2. Решение владельца от 28.08.2026 — целевой состав и порядок допуска KB**

Владелец утвердил (2026-08-28):

- **Целевой состав KB — все GitHub-репозитории видимой витрины** (`project_cards.is_visible = true`): 12 репозиториев на 13 карточек (HR-Assistant — `SHARED_REPOSITORY`, обслуживает карточки 8 и 9).
- **Порядок допуска — по `display_order` витрины** (реестр `project_cards`, не алфавит и не размер корпуса).
- Скрытые карточки (`telegram-ai-gateway`, `competitor-monitor-ai`) — **вне целевого плана** до отдельного решения владельца.
- Собственный корпус `AI-Portfolio` — вне текущей очереди допуска; подключение — отдельное решение владельца. **Решение владельца (28.08.2026, вторая половина дня): собственный корпус отложен** — подключение после финализации доработки AIP и документации проекта, непосредственно перед Deployment Validation; цикл «допуск → sync → reindex → eval» проходится один раз на зрелой документации.

| Очередь | Источник | Карточка (display_order) | Статус |
|---|---|---|---|
| — | AI-Curator | 1 | ✅ допущен 28.08.2026 (16 док / 424 чанка) |
| 1 | ai-data-assistant | 2 | ✅ допущен 28.08.2026 (286 чанков) |
| 2 | Review-Flow | 3 | ✅ допущен 28.08.2026 (428 чанков) |
| 3 | review-auto-responder | 4 | ✅ допущен 28.08.2026 (333 чанка) |
| 4 | Assistant-Flow | 5 | ✅ допущен 28.08.2026 (298 чанков) |
| 5 | meeting-audit-bot | 6 | ✅ допущен 28.08.2026 (198 чанков) |
| 6 | Lead-Qualification-MVP | 7 | ✅ допущен 28.08.2026 (403 чанка) |
| 7 | HR-Assistant | 8 (покрывает также карточку 9 LoRA) | ✅ допущен 28.08.2026 (1898 чанков) |
| 8 | telegram-intake-bot | 10 | ✅ допущен 28.08.2026 (19 док / 218 чанков) |
| 9 | telegram-onboarding-bot | 11 | ✅ допущен 28.08.2026 (13 док / 212 чанков) |
| 10 | Retail-Group | 12 | ✅ допущен 28.08.2026 (5 док / 108 чанков) |
| 11 | PromptReview | 13 | ✅ допущен 28.08.2026 (17 док / 447 чанков) |

**Итог кампании допуска (28.08.2026):** 12 источников, 211 документов, 5253 чанков в `ai_portfolio_knowledge_v2`, дубликатов 0; ассистент переключён на `v2`. Механика каждого прохода и полные протоколы (SHA, регистры, sync jobs, инциденты) — в документации проекта. Примечание: локальный remote у части кейсов-плейсхолдеров указывает на `telegram-intake-bot`; при допуске источник подтверждать по GitHub API, а не по локальному remote.

---

## 📈 3. Market Validation

AI Portfolio создан как публичная витрина компетенций. Конкретные заказы и сделки на данном этапе не зафиксированы; после запуска устанавливается baseline конверсии посетителей в обращения.

---

## 💼 4. Commercial Assessment

| Фактор | Оценка |
|--------|--------|
| Потенциал | Высокий — витрина закрывает типичный pain-point поиска исполнителя |
| Востребованность | Средне-высокая — заказчики AI-автоматизации оценивают портфолио |
| Риски | Низкая конверсия без чёткого позиционирования; зависимость от актуальности кейсов |

---

## 🛠️ 5. Key Technology Areas

| Область | Компетенция | Статус |
|---------|-------------|--------|
| FastAPI + PostgreSQL | Backend | ✅ |
| ChromaDB HTTP | Vector store | ✅ |
| OpenAI / GigaChat | Multi-provider LLM | ✅ |
| GitHub Sync | KB ingestion | ✅ |
| ProjectCard API | SOT карточек | ✅ |
| React Admin Console | Management UI | ✅ |
| Deployment on VPS | Production hosting | ✅ |
| Presale analytics | Conversion funnel | ⏳ Не реализовано |

---

## 📌 6. Decision

**Принято:** довести AI Portfolio до production-ready состояния к **04.09.2026** в соответствии с `docs/TZ.md` (утверждено) и остаточным `docs/IMPLEMENTATION_PLAN.md` от 27.08.2026.

**Утверждённые решения:**
- Итоговый проект курса — AI Portfolio.
- Целевая дата production-ready release: **04.09.2026** (не переносится).
- **Финальный публичный состав AI Portfolio — 13 полноценных проектов.** Prompt Review — полноценный finished-case с публичным репозиторием и страницей; placeholder-статус отменён.
- AI Portfolio является итоговым метапроектом/витриной и не входит в состав этих 13 проектов.
- Главная страница одновременно является витриной всех проектов; отдельный каталог проектов не является текущей основной витриной (`portfolio.html` и `/projects` переадресуют на `/`).
- Страницы «Услуги» и «Контакты» созданы и включены в общую навигацию; публичная поверхность в основном завершена.
- **Стандарт лендинга кейса:** `AIP Case Landing Page Standard v1.1` принят и задокументирован в `shared/patterns/aip-case-landing-page-standard.md`; фактический эталон — рабочая страница AI Curator (`/cases/ai-curator.html`). Файл `ai-curator-template-preview.html` — устаревший backup-шаблон, не эталон.
- **KB admission gate развёрнут в production, старый корпус пересобран (28.08.2026):** миграция `013 → 014`; KB-контур PostgreSQL очищен; создана чистая `ai_portfolio_knowledge_v2`.
- **Кампания диалогового допуска завершена (28.08.2026):** все 12 репозиториев видимой витрины допущены через admission gate в порядке `display_order` и синхронизированы в `ai_portfolio_knowledge_v2` (211 документов / 5253 чанков, dupes 0); состав каждого источника утверждён владельцем по регистру INCLUDE/EXCLUDE; внутренние, устаревшие и содержащие секреты документы в индекс не попали.
- **Ассистент переключён на `ai_portfolio_knowledge_v2` (28.08.2026, решение владельца):** через `CHROMA_COLLECTION_NAME` в compose; дефолт `config.py` не изменён (контракт теста конфигурации); после завершения кампании старая collection `ai_portfolio_knowledge` удалена — единственная KB проекта теперь `ai_portfolio_knowledge_v2`. Фикс `delete-before-discovery` в sync реализован (fail-closed: при ошибке discovery существующие документы сохраняются), покрыт тестами; два инженерных паттерна размещены в `shared/patterns/`.
- **Целевой состав KB утверждён (28.08.2026): все GitHub-репозитории видимой витрины** (12 репозиториев на 13 карточек; HR-Assistant — `SHARED_REPOSITORY` для карточек 8 и 9). Скрытые карточки (telegram-ai-gateway, competitor-monitor-ai) — вне плана до отдельного решения.
- **Порядок диалогового допуска источников — по порядку кейсов витрины** (`display_order`), начиная с `ai-data-assistant`; реестр очереди — §2.2 и документации проекта.
- **Целевой состав GitHub Sync:** 12 публичных проектных репозиториев — подключены и допущены (28.08.2026); собственный корпус AI Portfolio — **отложен решением владельца (28.08.2026)** до финализации документации проекта, подключение перед Deployment Validation (см. §2.2).
- **KB admission gate** обязателен для каждого источника: подключение репозитория не означает индексацию всех найденных Markdown-файлов; внутренние, устаревшие и неточные материалы в KB не допускаются.
- **Morphic** — направление следующего этапа ядра: диалоговый поиск и выбор релевантного кейса на главной странице. **Проба реализована 03.09.2026** (экран 1b: форма «опишите задачу» → `POST /case-match` → до 3 кейсов с why-объяснением; grounded, без кеша и сессий, откат — флаг `MORPHIC_ENABLED`); полный UX-контур (сессии, визард, сцена) — отдельное направление пост-релиза. Новые сервисы, сущности и архитектурные компоненты без отдельного решения владельца не добавляются.
- Baseline существующего технического долга зафиксирован внутренним аудитом корпуса (v1.2) — аналитический артефакт за пределами публичной сборки.
- Рейтинг проектов (v2.3) утверждён как внутренний аналитический артефакт (два рейтинга: 13 кандидатов и 12 проектов внутри портфеля) — за пределами публичной сборки.
- Шкала БЗ для внутреннего рейтинга: KB+RAG = 15, retrieval без RAG = 10, отсутствие = 0.
- Отсрочка Deployment Validation утверждена для: Telegram Intake Bot, Telegram Onboarding Bot, Review Flow, AI Portfolio. Для самого AI Portfolio Deployment Validation является обязательным критерием готовности публичного GitHub-проекта и входит в остаточный план. *(исполнение: закрыты все четыре — TIB и TOB 30.08, Review Flow 01.09, AI Portfolio 02.09, PASS)*
- На текущий момент DEFER CANDIDATE отсутствуют. Решение о любом переносе принимает только владелец.
- Production release выполняется на существующем VPS и домене `ai.alex-n8n.site`.
- **Позиционирование и B как поддерживающий продукт (02.09.2026):** продаём услуги AI-автоматизации (A — главное); платформа AI Portfolio (B) — поддерживающий продукт и доказательство инженерного уровня; 13 кейсов как самостоятельные активы (C) — фон. Формула продающего README: «услуги AI-автоматизации, доказанные 13 кейсами; живой ассистент — демо того, что заказчик получает».
- **Case Pipeline — утверждённое направление развития (02.09.2026, post-release, вне оценки §5):** конвейер «GitHub-репозиторий кейса → KB → лендинг на канвасе». Лендинг как данные (JSON по фиксированной схеме AIP Standard v1.1 в БД, хардкод `src/cases/*.html` уходит, `generate_landings.py` остаётся сидом/фоллбэком); системный промпт сборки лендинга №2 в управляемом хранилище `system_prompts` (развитие: prompt_kind либо вторая таблица; вход — допущенные документы кейса, выход — JSON от Hero до сценариев); контур доверия как в admission gate — immutable preview с commit SHA → утверждение владельцем → публикация. Развилки на отдельное решение: SVG как узлы/рёбра JSON, валидация выхода + конвенция статусов правила 12 в промпте сборки, показ пилотной карточки telegram-ai-gateway на витрине, порядок относительно Morphic. Реализация — после production release 04.09.2026 и отдельной команды владельца. Концепт — в документации проекта.

**Ожидающие решения владельца:**
- При фактической угрозе дедлайна 04.09.2026 — отдельное решение по DEFER CANDIDATE или другому сокращению scope.
- Что делать с кнопкой «Скачать портфолио» на главной странице (сейчас заглушка).

---

## ▶️ 7. Next Steps

1. ✅ Утвердить ТЗ (`docs/TZ.md`) и IMPLEMENTATION_PLAN.
2. ✅ Развёрнута dual-theme главная страница; 13 полноценных лендингов кейсов по стандарту `AIP Case Landing Page Standard v1.1` (эталон — `/cases/ai-curator.html`); страницы «Услуги» и «Контакты»; redirect устаревшего каталога на `/`.
3. ✅ Prompt Review финализирован как полноценный finished-case с публичным репозиторием и страницей кейса.
4. ✅ Актуализировать документацию источников и провести KB admission gate — все 12 источников допущены 28.08.2026 (протокол — в документации проекта).
5. ✅ Расширить KB до целевого состава — все 12 репозиториев витрины синхронизированы в `ai_portfolio_knowledge_v2` (211 документов / 5253 чанков, dupes 0); ассистент переключён на `v2`.
6. ✅ Настроить AI-ассистента на полный корпус; eval ≥90% / <5 сек; тюнинг retrieval/prompt — **выполнено 28.08.2026**: реестр 13 проектов в промпте, компакт-промпт `v4-compact-multi`, registry-only кеш; диагностическая eval-кампания (baseline + 5 re-runs): 159/168 = 94,6% формально, C-класс 68/77 = 88,3% после adjudication — принят владельцем (вариант «а»), latency p95 4956 мс < 5000 мс; протокол — в документации проекта; остаточно — формализация eval-артефактов в репозитории.
7. ✅ ~~Спроектировать UX-контракт Morphic-сцены~~ **Morphic-проба реализована 03.09.2026** (`POST /case-match` + сцена подбора на главной, `MORPHIC_ENABLED`-флаг отката); полный UX-контур (сессии, визард) — отдельное направление пост-релиза (см. `docs/ROADMAP.md`).
8. ✅ ~~Добавить presale-события и базовую presale-аналитику в Admin Console~~ **Выполнено 02.09.2026** — консоль «Аналитика → Пресейл»: `/track-visit`/`/track-event`, воронка с провалом вглубь, KPI-дельты, путь гостя. ~~реализовать UI управления допуском источников KB (§4.5а плана — до тестирования).~~ **§4.5а выполнено 28.08.2026** — Admission Console на `/content/sources` (draft/effective-правила, immutable-превью с commit SHA, stale-защита одобрения, block/unblock с аудитом, двойная защита PATCH; сценарии 23/23 PASS — протокол документации проекта).
9. ⏳ Пройти тестирование (функциональное, E2E, AI eval, ссылки, security, graceful degradation) и production release к **04.09.2026**. Прогресс: Deployment Validation PASS (02.09), unit-сьют 239 passed (03.09), релизная полировка 03.09 (Morphic-проба, панель документа, фиксы допуска, hero-кейсов), полный комплект документации (03.09, см. `docs/ROADMAP.md` и раздел «Связанные документы»).
10. ~~Deployment Validation AI Portfolio в чистом окружении + отчёт~~ ✅ Выполнено 02.09.2026, PASS (dind, 3 прогона fresh-from-zero, гайд v1.1); остаточно — ревизия поставки (`.gitignore`, внутренние артефакты) и финализация публичного репозитория `AlexLvGulyaev/AI-Portfolio`.
11. ⏳ **Бэклог (03.09.2026): KB-sync не вычищает чанки-сироты.** Sync переиндексирует только файлы, выбранные допуском; чанки документа, выпавшего из include-паттернов (или исключённого после одобрения), остаются в векторном индексе — сверка `verify` проверяет недостающие store-документы, но не лишние. Проявилось 03.09.2026: после вывода `Retail_Group_Case_Story_Scenario.md` из допуска Retail-Group 54 чанка остались в выдаче (источник «· Retail_Group_Case_Story_Scenario»), разово удалены штатным `delete_document_chunks` по составному ключу. Фикс: на этапе post-sync-сверки дополнительно вычислять лишние store-документы (store-ids минус expected) per-repo и удалять их (или хотя бы подсвечивать в `job.stats.verify` как `orphan_in_store`). Коммит `66609d4` (правка витрины 03.09) — последняя из серии 03.09; фикс — отдельной задачей в очереди работ.

---

## 🔗 8. Dependencies

| Зависимость | Описание | Влияние |
|-------------|----------|---------|
| VPS + домен | Production hosting | Блокирует публичный доступ |
| PostgreSQL | ProjectCard, logs, sessions | Блокирует backend |
| ChromaDB HTTP | Векторный поиск | Блокирует AI-ассистента |
| OpenAI API | Основной LLM | Блокирует AI-ассистента |
| GigaChat API | Fallback LLM | Деградация при недоступности OpenAI |
| GitHub API | Синхронизация KB | Блокирует актуализацию знаний |
| Согласование SOT | Утверждение плана и scope | Блокирует старт массовой реализации |

---

## ⚠️ 9. Risks

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Документация портфельных проектов не доведена до стандартов APL — попадание в KB устаревших/внутренних/противоречивых материалов | Высокая | Высокое | KB admission gate по каждому источнику; при отсутствии пригодного корпуса — актуализация минимального канонического публичного документа; выборочный состав индексации |
| Полный остаточный объём (KB + AI-ассистент + Morphic + presale-аналитика + тестирование + Deployment Validation) не укладывается до 04.09.2026 | Средняя | Высокое | Приоритет Must Have; DEFER CANDIDATE — только по решению владельца |
| ChromaDB reindex медленный | Средняя | Среднее | Инкрементальная синхронизация; ночной запуск |
| C-класс корректности стохастически колеблется между прогонами (принятый владельцем итог overall — 94,6%, C-класс 88,3%) | Средняя | Среднее | Принято владельцем как достигнутый уровень (28.08.2026); повторные полные прогоны — только по согласованию |
| Запас production p95 до лимита 5 секунд минимален (4956 мс; единичные выбросы >5 с) | Средняя | Среднее | Мониторинг latency; при деградации — декомпозиция (retrieval/генерация) по трейсам |
| ~~LLM-ответы временно не кешируются (registry-only политика после устранения контаминации кеша)~~ ✅ Закрыт 04.09.2026 — кеш возвращён со структурным признаком cache-eligible (grounded-ответы: rag_used, без отказа и fallback, первый вопрос сессии; требование цитаты `[N]` снято решением владельца 04.09) | — | ~~Низкое~~ Снято | Fingerprint-ключ инвалидирует кеш при смене промпта/модели/KB; повторные идентичные вопросы в новых сессиях отвечаются из кеша (~70 мс) |
| Retrieval зависит от OpenAI embeddings; runtime fallback retrieval не реализован и отложен решением владельца | Низкая–средняя | Среднее | Варианты решения (graceful degradation / второй embedding-провайдер / локальная модель) зафиксированы в документации проекта; решение владельца (28.08.2026) — пока не реализовывать |
| Устаревшие ссылки | Высокая | Среднее | Автопроверка, placeholder-страницы |
| Расхождение документации и реализации | Средняя | Среднее | Регулярная синхронизация SPEC/TZ/IMPLEMENTATION_PLAN при изменении scope |

---

## 🕒 10. Status History

| Дата | Статус | Комментарий |
|------|--------|-------------|
| 2026-07-12 | Создан проект | Первый PROJECT_STATE, SPEC, IMPLEMENTATION_PLAN |
| 2026-07-13 | UI реализован | 4 страницы + страницы кейсов |
| 2026-07-14 | Backend v1 | FastAPI + PostgreSQL + chat endpoint |
| 2026-07-15 | Admin Console v1 | Dashboard, Content/KB, Logs |
| 2026-07-19 | GitHub Sync | 7 источников, 192 документа, ~5400 чанков |
| 2026-08-18 | Финальное планирование | TZ v1.2, IMPLEMENTATION_PLAN v3.1 утверждены; внутренние рейтинговые и аудит-артефакты (v2.3 / v1.2) утверждены; релиз 04.09.2026 (status quo ante) |
| 2026-08-25 | Новая главная страница + расширение до 13 карточек | Развёрнута пилотная dual-theme главная с 13 карточками; нумерация ProjectCard зафиксирована; чат-виджет и темы работают; production URL `https://ai.alex-n8n.site/`; `SPEC.md` v2.1, `TZ.md` v1.4 и `IMPLEMENTATION_PLAN.md` v3.2 актуализированы под 13 карточек и логику featured/archive; каталог, страницы кейсов, AI-ассистент и presale-аналитика ещё не завершены |
| 2026-08-25 | Принят UI-baseline страницы кейса | Принят `AIP Case Landing Page Standard v1.1`; эталонная реализация — dual-theme лендинг AI Curator; задокументировано в `shared/patterns/aip-case-landing-page-standard.md` |
| 2026-08-26 | Все 13 лендингов приведены к стандарту AIP v1.1 | Генератор `build_registry.py` + `generate_landings.py`; финальные правки Retail Group (#12) и Assistant Flow (#5); AI Curator — ручная страница (`MANUAL_CASES`) |
| 2026-08-27 | Публичная поверхность в основном завершена | Созданы и включены в навигацию страницы «Услуги» и «Контакты» (mirror-bento layout); устаревший каталог `/portfolio.html` и `/projects` переадресуют на `/` (главная = витрина всех проектов); Prompt Review — полноценный finished-case; коррекция диагноза: эталон шаблона — `/cases/ai-curator.html`, а не `ai-curator-template-preview.html` |
| 2026-08-27 | Актуализация SOT под состояние на 27.08.2026 + направление Morphic | Зафиксировано: 13 полноценных проектов; целевой состав GitHub Sync = 12 проектных репозиториев + собственный корпус AI Portfolio (проверено по живому состоянию GitHub; репозитории `Retail-Group` и `AI-Portfolio` публично существуют); введён KB admission gate; зафиксирован риск качества проектной документации; Morphic — согласованное, не реализованное направление ядра (UX-механика — отдельная задача); `PROJECT_STATE`, `TZ.md` v1.6, `SPEC.md` v2.3, `IMPLEMENTATION_PLAN.md` v4.0 актуализированы |
| 2026-08-28 | Production-пересборка KB + утверждение целевого состава и порядка допуска | Развёрнут KB admission gate (миграция 013→014), backend задеплоен; старый KB-контур PostgreSQL очищен, старая collection `ai_portfolio_knowledge` (5400 чанков) продолжает обслуживать сайт; создана `ai_portfolio_knowledge_v2` с AI Curator (16 док / 424 чанка, идемпотентность подтверждена); владелец утвердил целевой состав KB = все репозитории видимой витрины (12 репозиториев / 13 карточек) и порядок допуска по `display_order`, начиная с `ai-data-assistant`; очередь — §2.2, протокол — в документации проекта |
| 2026-08-28 | KB admission campaign завершена, ассистент переключён на v2 | Все 12 репозиториев витрины допущены в порядке `display_order` и синхронизированы в `ai_portfolio_knowledge_v2`: 12 источников / 211 документов / 5253 чанков, дубликатов 0; состав каждого источника утверждён владельцем по регистру INCLUDE/EXCLUDE; ассистент переключён на `v2` через `CHROMA_COLLECTION_NAME` (дефолт `config.py` не изменён; старая collection сохранена как откат); backlog выполнен: fail-closed фикс `delete-before-discovery` с тестами + 2 паттерна в `shared/patterns/`; протокол — в документации проекта |
| 2026-08-28 | Пост-кампанный день: безопасность, фоллбэк-движок, удаление старой KB | Ротация GitHub-токена (перевыпуск, старый удалён из GitHub, fingerprint-проверка контейнера); ротация `LANGFLOW_SECRET_KEY` кейса prompt-review (Fernet-ключ в `infra/.env`/`api/.env` без вывода в чат; маскировка и пуш `498d6de` в публичный PromptReview); CORS-фикс LangFlow; LangFlow провалидирован как фоллбэк-движок полным прогоном (пересборка не нужна), production возвращён на LangChain — решение владельца: LangChain основной, LangFlow фоллбэк, README актуализирован; старая collection `ai_portfolio_knowledge` (5400 чанков) удалена — единственная KB проекта `ai_portfolio_knowledge_v2`, ассистент проверен |
| 2026-08-28 | Диагностическая eval-кампания выполнена, результат принят владельцем; собственный корпус отложен | Диагностическая eval-кампания production-ассистента (baseline + 5 re-runs, 174 теста/прогон): устранены provenance-дефекты, кросс-сессионная контаминация кеша (registry-only policy, KEY_SCHEMA `v4-registry-only`), дрейф памяти; промпт `v4-compact-multi` с детерминированным реестром 13 проектов; итог 159/168 = 94,6% формально, C-класс 68/77 = 88,3% после adjudication — принят владельцем (вариант «а»), latency p95 4956 мс < 5000 мс; eval-данные удалены из production БД с разрешения владельца; KB admission gate закоммичен (`f899f24`), документация актуализирована (`7ed8657`). Решения владельца: собственный корпус AI Portfolio (13-й источник) отложен до финализации документации, подключение перед Deployment Validation; риск отсутствия fallback-канала retrieval зафиксирован без реализации; протокол — в документации проекта |
| 2026-08-28 | §4.5а Admission Console реализована и задеплоена | Admission Console на `/content/sources` (три колонки 25/50/25, GitHub-репозиторий — единственный тип источника, draft/effective-модель правил, state machine допуска, immutable-превью с commit SHA, stale-защита одобрения patterns/commit, block/unblock с аудитом событий, двойная защита прямого PATCH 409 `use_admission_actions`); миграция 015 (kb_admission_previews, kb_admission_events, draft-колонки) применена к production; старая панель KnowledgeSourcesPage удалена; unit-тесты гвардов 23/23, мутационные сценарии на тестовом источнике 23/23 PASS (сценарии выполнены после восстановления GitHub-доступа: ротация токена), затем тестовый источник, его превью и события удалены; production-состав 12 источников (approved, паттерны) не изменён. Протокол — в документации проекта |
| 2026-08-29 | Очередь владельца §4.5б; Retrieval-консоль: визуальный канон + WH-1/WH-2 (кеш поиска, runtime-параметры) | Владелец зафиксировал очередь из 5 задач (§4.5б IMPLEMENTATION_PLAN v4.6). Позиция 1: Retrieval-консоль (`/system/retrieval`) переведена на визуальный канон «Системных настроек» (Page/ dashboard-section/ admin-status/ admin-btn, токены `--admin-*`) и задеплоена; приёмка визуала — за владельцем. Затем по прямой отмашке владельца («Делаем, конечно») реализовано §7.1 (v4.7→v4.8): WH-2 — runtime-параметры AF (`rag_answer_max_tokens`, `rag_retrieval_timeout`, `rag_embedding_request_timeout`) в tuning без reindex; WH-1 — retrieval cache: sqlite-хранилище, детерминированный SHA-256 ключ (вызов + backend + runtime-тюнинг + embedding-модель + generation), подключение на уровне `get_backend()` (публичный чат + admin chat-preview), тумблер/очистка через admin API, generation++ после успешного KB-синка, панель в консоли справа от «Индексации», переопределения/пути — collapsed-панель внизу (решение владельца). Unit 8/8 новых + регресс, live-проверки admin API на проде, фронт/бэк пересобраны. Протоколы — в документации проекта |
| 2026-08-30 | Позиция 1 §4.5б закрыта (консоль Retrieval принята владельцем); публичный eval-сет 24/24 на weaviate @ 900/120 | Консоль `/system/retrieval` одобрена по совокупности: визуальный канон + WH-1/WH-2 (кеш поиска, runtime-параметры), аудит рантайм-параметров (8/10 полей функциональны; `chroma_ef_*` — chroma-only no-op при активном weaviate; константы диверсифицированных маршрутов оставлены структурно), живая приёмка retrieval cache (тумблер без рестарта, hit 1233→0 мс с идентичным ранжированием, инвалидация generation после KB-синка; кеш отделён от LLM-ответов — registry-only политика в силе; возвращён в off/clean). Накануне: KB-sync получил громкий отказ частичных батч-ошибок + пост-сверку индекса с PG (job.stats.verify), первый публичный прогон 21/24 отдебажен (раннер с якорением сессии, явное отрицание скрытой карточки), принятый чистый перепрогон **24/24, p95 2599 мс** (AI_EVAL_REPORT §9). Код не коммичен — по команде владельца. Следующая позиция очереди §4.5б — 2: консоль AI-настроек (системный промпт вне хардкода). Протокол — в документации проекта |
| 2026-08-30 | Позиция 2 §4.5б выполнена и развёрнута (ожидает приёмки владельцем): консоль AI-настроек + перестройка «Системы» по 8-пунктовой постановке владельца | Системный промпт вынесен из хардкода: таблица `system_prompts` (миграция 021 — single-active через partial-unique, дедуп по паре «метка+body_hash», история версий, вшитый v4-compact-multi как источник сброса), `SystemPromptService` + API (`GET/PUT /admin/system-prompt`, reset, activate), `ChatOrchestrator` подгружает активный промпт из БД (fail-open к вшитому), fingerprint `PromptAssembly` инстансный → смена промпта автоматически инвалидирует ответ-кеш. Консоль `/system/ai` «AI-настройки»: слева панель LLM-провайдеров (перенос без изменения конфигурации), справа равная панель «Системный промпт» (версия, редактор, исторические версии с «Сделать активной» + confirm на вшитой, сброс к вшитому, зелёный notice после действий, подсветка активной строки — UX-доработка после инцидента клика по «Активировать»). «Системные настройки» реорганизованы в группу меню «Система» (Обзор / Retrieval / AI-настройки; страница состояния — «Обзор»): 4-й бэкенд Weaviate в «Состоянии системы», «Документы» (225) в «Контент» (Last Sync в 3-й строке), «Аудит» (37 событий) в «Операции» (Updated в 3-й строке). Названия решены владельцем (из предложенных, финально): консоль = «AI-настройки», группа меню = «Система», страница состояния = «Обзор». Живая приёмка на проде: dashboard-метрики (weaviate ok, documents 225, chunks 3285), round-trip промпта (save→activate→/chat→reset→/chat), 422 на тело без плейсхолдеров; 176 unit-pass (5 — известные smoke с живой БД); тестовые строки приёмки удалены; финальное состояние по решению владельца — активна управляемая **v5-compact-multi** (тело = вшитому, hash e706cfe4; источник истины — БД, вшитый текст остаётся fallback-ом). Осталось: визуальная приёмка владельцем, актуализация DEPLOYMENT_GUIDE, коммит по команде. Обязательный контрольный eval после смены промпта отменён владельцем (30.08.2026). Файл задачи — в документации проекта; следующая позиция — 3 (консоль «Документы») |
| 2026-08-30 | Позиция 3 §4.5б выполнена и развёрнута (ожидает приёмки владельцем): консоль «Документы» | Консоль `/content/documents` по постановке владельца (референс AIC «База знаний», упрощённо, без жизненного цикла): слева айтемы документов (225, поиск, бейдж чанков), справа узкая панель «Активный бэкенд» (WEAVIATE · 3285 · ok) и карточка: ПАСПОРТ → ЭКСПЛУАТАЦИЯ (чанки в активном бэкенде, «в/не в индексе», обновление) → Превью текста с кнопкой «Открыть» (полный текст в модале) → Чанки, упорядоченные по chunk_index. Backend read-only: `GET /admin/knowledge-base/documents` (+ `/{id}`, `/{id}/text`, `/{id}/chunks`), сервис `DocumentsConsoleService`; паритетные методы `count_document_chunks` / `chunk_counts_by_document` / `list_document_chunks` в `WeaviateBackend` и `RAGService`. Ключевая находка: в векторных сторах `document_id` — это составной ключ синка `github_{identifier}_{path}` (IndexerDocument.id), а не PG UUID — маппинг PG-документов на чанки идёт через этот ключ. 11 unit-тестов (suite: 188 pass / 5 известных smoke), живая приёмка API на проде (225 документов, 100 в индексе, 2410 чанков по документам; карточка AI-Curator docs/API_CONTRACT.md: 71 чанк, полный текст 55 323 симв.) + DOM Playwright (роут, активная nav, панели по порядку, модал полного текста). Протокол — в документации проекта; следующая позиция §4.5б — 4 (консоль «Пресейл», = §4.5) |

---
| 2026-08-30 | Позиция 3 §4.5б переделана v1→v2 по настоящему AIC-референсу (ожидает приёмки владельцем) | В v1 консоли «Документы» референс был ошибочно подменён Streamlit-UI assistant-flow (владелец: «AIC — стримлит?????»); настоящий AIC = `cases/ai-curator/admin-console` (React). Переделка по директивам владельца: активный бэкенд — тулбар-стрип `ac-strip` во всю рабочую область (канон «Источники и синхронизация»: бейдж WEAVIATE, store AiPortfolioChunk, «3285 чанков · 225 документов»); в левой макропанели — фильтры «Все источники» + «Все форматы» (расширение пути), поиск, пагинация AIC по 7, 3-строчные айтемы (дата, бейдж в/не в индексе, title, «N чанков · источник»), счётчик показывает ВСЕ документы (корпусный `total_documents`); справа — две макропанели без третьей: сетка «Паспорт | Эксплуатация» на CompactRow-строках (Паспорт: ID/Название/Путь/Источник/URL/Commit; Эксплуатация: Чанки в бэкенде/Статус/Объём/Загружено/Обновлено — поля согласованы владельцем), «Preview текста» (кнопка «Открыть» → модал полного текста, счётчик «N / M») → «Чанки (N)» с «Раскрыть/Свернуть» (line-clamp-3 ↔ разворот). Два корневых фикса: (1) weaviate aggregate `group_by="document_id"` без явного limit обрезался серверным лимитом 100 групп — 125 документов показывали «—», а «в индексе» числились только 100 из 225; `GroupByAggregate(prop="document_id", limit=10_000)` — теперь корректны все 225; (2) счётчик «N / M» превью счит UTF-16 код-юниты (JS `string.length`): 1606 против 1600 код-поинтов — длина превью теперь отдаётся сервером (`text_preview_length`). Приёмка Pass: 11/11 unit-тестов (suite 188/5 pre-existing), tsc clean, live API (225 документов, источник-фильтр 14, формат-фильтр: корпус — весь md), DOM Playwright (стрип во всю ширину 0/0, «Страница 2 из 33», модал 6320 симв., сворачивание/разворачивание чанка). Урок по «100 vs 225» — лимит групп aggregate для сторов с BYOV-метаданными. Протокол (раздел «Переделка v1 → v2») — в документации проекта; следующая позиция §4.5б — 4 (консоль «Пресейл», = §4.5) |
| 2026-08-30 | Аудит вынесен в отдельную консоль «Аудит»; группа меню «Операционная консоль» (по команде владельца перед следующей консолью очереди) | В «Логах» аудит был вторым табом — извлечён в отдельную страницу `/audit` (`AuditPage.tsx`): окно 24h/48h/7d, фильтры «тип события» (admin_login/site_visit/provider_switch/прочее) и «статус», пагинация по 7, айтем (время МСК, бейдж типа события, статус, превью ip·UA / query·visitor_id / provider·model), правая панель «Audit-запись» (kv + metadata JSON). «Логи» остались execution-сессиями (табы удалены, лид «Operational console · execution-сессии · время: МСК»). Навигация: «Логи», «Диалоги», «Аудит» объединены в группу меню **«Операционная консоль»** (канон группы «Система»). Backend не менялся (тот же API `/logs`). Приёмка DOM Playwright на проде: на /audit фильтры работают (site_visit → 0, 7d → 78 записей, «Страница 2 из 12»), /logs без табов, группа подсвечивается активной. Протокол — в документации проекта; следующая позиция очереди §4.5б — 4 (консоль «Пресейл», = §4.5) |
| 2026-08-30 | Аудит админ-мутаций по канону AIC (по постановке владельца после сравнения с ai-curator, «по умолчанию») | Хелпер `log_admin_action` (`app/api/admin/audit.py`, fire-and-forget): event_type `admin_action`, source `admin_console`, query «action resource_type», metadata = action/resource_type/resource_id/changed_fields (только имена полей)/ip/user_agent/path/details; ошибка аудита не ломает действие. Инструментированы все мутации консолей: knowledge_base 13 (kb; карточки — project_card), retrieval 5 (retrieval_tuning), ai_providers 4 (ai_config), prompt 3 (system_prompt). Консоль «Аудит»: опция фильтра «админ-действия», превью admin_action = query + resource_id. Приёмка: 8 новых unit-тестов (suite 196/5 pre-existing), live API (cache_clear и system-prompt/reset → записи в /admin/logs?event_type=admin_action), DOM Playwright (фильтр, бейдж «Админ-действие», metadata JSON в карточке). Протокол — в документации проекта; следующая позиция очереди §4.5б — 4 (консоль «Пресейл», = §4.5) |
| 2026-08-30 | Местное время зрителя во всех отображениях времени админ-консоли (уточнение владельца: «не московское, а местное») | В `operationalLabels.ts` убран принудительный `Europe/Moscow`: `formatTimestampMsk` → `formatTimestampLocal` / `formatDateLocal` / `formatShortDateTimeLocal` (Intl ru-RU без timeZone). Корневая находка: бэкенд отдаёт наивные UTC-строки (PG `Etc/UTC`, без суффикса зоны) — добавлена нормализация (время без смещения = UTC, `+Z`), иначе браузер вне UTC читал бы время неверно. На общие форматтеры переведены все страницы: Логи/Диалоги/Аудит (лиды «время: местное»), Обзор (Last Sync/Updated), AI-настройки, Документы, Карточки, Допуск. Приёмка DOM Playwright в двух зонах (Europe/Moscow → 10:40:59, America/New_York → 03:40:59 для одной и той же записи). Протокол — в документации проекта |
| 2026-08-30 | Мета-карточка платформы «Это Я» в реестре project_cards (постановка владельца; «Мета-карточка платформы. Жесткий 400») | AIP получил карточку в собственном реестре — исключительно для управления своей документацией и будущим включением в KB (13-й источник). Миграция 022 (`project_cards.is_meta`, NOT NULL, default false); карточка slug `ai-portfolio`, category `meta`, is_visible=false, show_on_homepage=0, создана через admin API. Семантика исключения из витрины и знаний: чат-реестр (PortfolioRegistry) не включает мета-карточку НИ в одном режиме (публичный и admin include_hidden); публичный каталог фильтрует по is_visible. Защита по утверждению владельца: DELETE → 409 `meta_card_protected` (жёстко), PATCH параметров дисплея (is_visible, show_on_homepage, display_order, slug) → жёсткий 400 `meta_card_display_locked` с перечнем полей; текстовые поля свободны. Консоль: бейджи «Мета-карточка платформы»/«МЕТА-КАРТОЧКА», поля дисплея скрыты из формы, slug disabled, удаление disabled с tooltip. Попутный вопрос владельца «как чат-агент покажет привязанные документы?» вскрыл коллизию с гвардом B1 (29.08): репозитории невидимых карточек вырезаются из публичной выдачи — метапак был бы похоронен; утверждён вычет `is_meta = false` из обоих B1-запросов (метапак — публичные знания зрителя, не отложенный релиз). Приёмка: миграция 021→022 в проде, API-гварды 409/400/200 живьём, публичный каталог без карточки (13 карточек), audit-следы create/update, DOM Playwright (бейджи, disabled, tooltip, скрытые поля), 9 новых unit-тестов (suite 209/5 pre-existing). Урок: рабочие миграции — только в `backend/migrations/versions/` (копия у корня кейса устарела). Протокол — в документации проекта; следующая связанная фаза — ревизия публичного репозитория (§4.7), затем допуск 13-го источника |
| 2026-08-30 | Фикс 422 при построении состава допуска (источник AI-Portfolio; сообщил владелец) | Root cause: репозиторий на ветке `master`, а UI допуска префиллится `branch='main'` → GitHub API `/commits/main` → 422. Немедленный фикс: PATCH источника → `branch=master` (состав строится, commit 30826e31). Корневой фикс: `create_source` при ветке-плейсхолдере «main» подставляет дефолтную ветку репо из GitHub (новый `default_branch()`-проб в GitHubKnowledgeSourceService); явно выбранные ветки не перезаписываются, прямой API без branch — прежнее поведение. +3 unit-теста (suite 212/5 pre-existing), деплой, живое подтверждение. Открытый вопрос перед одобрением допуска: дефолтный драфт допускает README.md + docs/SPEC.md (внутренний артефакт §4.7 — в KB нельзя), метапак в состав пока не входит; паттерны → консоль допуска, ревизия публичного репо (§4.7) — до одобрения. Протокол — в документации проекта |
| 2026-08-30 | KB-синк в активный chroma: починен Write-путь, выполнен полный ресинк (сообщил владелец) | Root cause падения синка 13-го источника: активный бэкенд — chroma (владелец переключил целенаправленно, audit 17:20), а `index_store_for` требовал у бэкенда контракт записи (`add_chunks`), который у legacy `RAGService` отсутствует (переехал в WeaviateBackend) → предусмотренный фолбэк на `ChromaIndexStore` стал недостижим. Фикс: бэкенд без контракта записи, являющийся `RAGService`, оборачивается в `ChromaIndexStore` (+2 теста, suite 214/5 pre-existing). Вопрос владельца «какая разница, когда переиндексировать» закрыт: результат идемпотентен, но полный ресинк выбран ради однородного корпуса под текущий тюнинг 900/120. Ресинк job 6c5ae49e — success: 230 документов, 3370 чанков, 14 источников; метапак AIP индексирован по паттернам допуска (README, FAQ, GLOSSARY, PORTFOLIO_OVERVIEW, COMPETENCIES — 44 чанка, repo-bound `AlexLvGulyaev/AI-Portfolio`, B1-вычет для мета-карточки в силе). Контрольные вопросы чата: «Что такое RAG?» отвечает, «Как связаться с вами?» и «Что такое n8n?» — честные отказы. Диагноз: разбавление чанков фиксированным окном — FAQ-раздел контактов (cosine 0.33) и n8n-секция глоссария (вне топ-120) проигрывают топ-6 чат-ретрива; изолированная секция дала бы 0.65 (абсолютный лидер). Целевой фикс — heading-aware чанковка — вынесен на решение владельца (меняет пайплайн + полный ресинк). Побочная находка: конвертер markdown→plain режет теги `<[^>]+` без `>` — мусор в текстах чанков, ранжирование не затрагивает. Коммит — по команде владельца. Протокол — в документации проекта |
| 2026-08-30 | Heading-aware чанковка KB (постановка владельца: документы-источники — SOT, «если проблема в механизме чанкования, значит, её нужно решать») | Root cause честных отказов на «Как связаться с вами?» / «Что такое n8n?» (диагноз предыдущей записи): окно 900/120 режет и разбавляет узкие секции — FAQ-контакты (254 симв.) внутри 900-симв. чанка (cosine 0.33 против 0.45 у победителей топ-6, rank 72), n8n-секция глоссария вне топ-120 (изолированно — 0.65, абсолютный лидер). Фикс: `_create_chunks` режет по заголовкам `^#{1,6}` — секция ≤ chunk_size идёт цельным чанком (склейки секций нет), переростки — прежним окном; документы без заголовков — прежнее поведение. Вопрос владельца про LangChain: `MarkdownHeaderTextSplitter` даёт ту же способность, но заимствован алгоритм, не библиотека — в русле прежнего решения о переносе LangChain. В комплекте исправлен конвертер md→plain (`<[^>]+` → `<[^>]*>` — мусорные `>` в текстах чанков; на ранжирование не влиял) + сохранение уровней заголовков. Тесты: +5 (`test_heading_aware_chunking.py`), суит 219/5 pre-existing. Полный ресинк job 9cd8fd87 (success): 230 док., 7656 чанков (было 3370 — рост за счёт атомарных секций), AIP-метапак 94 чанка. Контрольные вопросы — 3/3: contacts прямо из FAQ, n8n из глоссария, RAG с указанием источников. Коммит — по команде владельца. Протокол — в документации проекта |
| 2026-09-02 | DEPLOYMENT_GUIDE создан, Deployment Validation пройдена (PASS) | `docs/DEPLOYMENT_GUIDE.md` v1.0 создан из фактического состояния (коммит `f919155`), затем валидирован запуском в чистом окружении — dind-стенд `aip-dv-dind` (Docker 29.7.2, named volume по паттерну disk-flooding), свежий клон публичного GitHub, секреты перенесены программно без чтения значений. 3 прогона fresh-from-zero: №1 FAIL — дефект цепочки миграций (сид 003 зависел от колонки show_on_homepage, добавляемой 004; на проде не проявлялся из-за инкрементального применения; фикс `c00bb6a`), №2 FAIL — расхождение с продом (013 прятал канонический prompt-review, 12 видимых против 13; фикс `48f8751`), №3 PASS — полный прогон §4: 5/5 контейнеров healthy, 22/22 миграций, 13 видимых карточек = прод, KB bootstrap через Admin API (мета-карточка + источник AlexLvGulyaev/AI-Portfolio, draft-паттерны с прода, preview 5/15, approved, sync 5 документов / 113 чанков, verify чист), живой чат с источниками за 2,9 с. Гайд актуализирован до v1.1 (`398163a`); хвост `assistant-flow.html` (кнопки Admin Console) закоммичен и запушен (`6c9a822`). По находкам гайда дополнен: сид реестра, draft-правила до Preview, мета-карточка платформы. Стенд внесён в реестр ресурсов VPS (§1 порты 18000/18080 + карточка 4.18, `d49dbdf` в лаб-репо, снос — по готовности владельца). Протокол — в документации проекта; следующая позиция §4.7 — 7.2 (ревизия .gitignore/поставки) → 7.3 → 7.4 (release 04.09) |
| 2026-09-02 | Системный промпт: экспорт в публичную документацию; позиционирование и направление Case Pipeline | PROMPT_ARCHITECTURE.md (v1.1) создан и запушен (`ff72f4b`) — закрывает сдачу PEf05 «системный промпт (text)»: PromptAssembly/плейсхолдеры, управляемое хранилище (миграция 021), эволюция v4→v5→v6→v7(r1–r4)→v8, verbatim-экспорт активной копии БД `v8-no-match-honesty` (БД — SOT; вшитый fallback отстаёт только в косметике, выравнивание — код к БД по команде). По обсуждению позиционирования владельцем принято: продаём услуги AI-автоматизации (A главное), платформа (B) — поддерживающий продукт, 13 кейсов (C) — фон. Зафиксировано направление развития **Case Pipeline** (post-release, вне оценки §5): конвейер «GitHub-репо кейса → KB → лендинг на канвасе»; лендинг как данные (JSON по AIP Standard v1.1), промпт сборки №2 в system_prompts, контур доверия admission-style (preview → утверждение → публикация); пилот — telegram-ai-gateway; развилки (SVG узлы/рёбра, валидация + правило 12, показ пилота) — отдельно. Концепт — в документации проекта. Открыто: ревизия поставки 7.2/7.3 (продающий README и пакет документации — исследование выполнено, состав согласован с владельцем частично) |
| 2026-09-03 | Роутинг «страница = названный проект» + маршруты проверки демо (DEMO_ROUTE) в KB всех 13 кейсов | По репорту владельца (ответ ассистента со страницы AIC на чип «что проверить» — поток возможностей + чужие источники): заменён мягкий буст варианта B на роутинг — валидный page_slug резолвится в карточку реестра и работает как явно названный проект (retrieval project_scoped по репо страницы с честным fallback в глобальный поиск; явно названный проект приоритетнее; скрытые карточки в публичном реестре отсутствуют). В блок «КОНТЕКСТ СТРАНИЦЫ» добавлена доверенная инструкция формы: «что проверить в демо» → нумерованный маршрут «открыть → сделать → увидеть». Контент маршрутов — 13 публичных документов `DEMO_ROUTE.md` в 12 репозиториях кейсов (HR-Assistant — два: бот и LoRA-лендинг), все согласованы владельцем по одному, закоммичены и запушены; в KB admission 12 источников расширены include-паттернами (draft → preview ready → approve), полный sync: 243 документа / 7733 чанка / 14 источников. Живая верификация: page_slug ai-curator / ai-data-assistant / hr-assistant-lora / telegram-intake-bot — источники project-scoped с «· DEMO_ROUTE» в топе, ответы нумерованным маршрутом, реальные URL; побочный дефект (модель выдумывала URL демо) закрыт реальными ссылками в DEMO_ROUTE. Правки витрины по SOT-расхождениям: meeting-audit-bot (демо «по запросу» → живой бот @PEcb10_bot + демо-вход админки), telegram-intake-bot (диалог по вопросам вместо «заявки свободным текстом»), telegram-onboarding-bot (обучение + тест из 5 вопросов с оценкой каждого ответа вместо «адаптивных вопросов»; заголовок страницы и og/twitter-title → «онбординг с проверкой знаний»), фронт пересобран и проверен в проде. Код бэкенда и правки страниц ai-portfolio — коммит по команде владельца. Протокол — в документации проекта |
| 2026-09-03 | Разведение демо-вопроса и устройства кейса; память диалога (v10→v12); тултипы статусов; фикс фильтров; актуализация маршрута Lead Qualification | Продолжение темы маршрутов по репортам владельца. (1) Одинаковый ответ ассистента на «что проверить в демо» и «как устроен кейс» — триггер формы в блоке «КОНТЕКСТ СТРАНИЦЫ» сужен до демо-вопросов, «как устроен» → содержательный архитектурный ответ; гигиена шагов маршрута (только действия зрителя, без внутренних проверок — Markdown-рендер, localStorage-история, которые модель натаскивала из FAQ/USER_GUIDE/API_CONTRACT): промпт **v10-demo-form-hygiene**. (2) Статусы главной: строка-легенда (едва заметна, уезжает при скролле) заменена тултипами на чипах карточек — hover/тап/фокус с клавиатуры, скролл пере-позиционирует тултип, ховер только для pointerType mouse (синтетический mouseover тач-браузеров ломал тап); Lead Qualification → «Живое демо» (живой стенд lead-qual.alex-n8n.site противоречил статусу «Кейс»); фильтры технологий починены — регистрозависимое сравнение `telegram/fastapi/rag/voice` против тегов БД «Telegram Bot»/«FastAPI»/«RAG»/«Voice AI» скрывало все карточки, сравнение приведено к нижнему регистру; Playwright-верификация всех шести фильтров. Коммиты `27f5134`, `af039cd` запушены. (3) Память диалога: по пейстам владельца (повтор сказанного на «что ещё», потеря имени «А меня как зовут?») — пайплайн памяти исправен (session_id переиспользуется, chat_messages пишутся, `{conversation_history}` в промпте); дефект в промпте: «ЖЁСТКОЕ ПРАВИЛО 3 — история НЕ источник фактов» (писалось против галлюцинаций о проектах) перебивало v11-секцию «ПАМЯТЬ ДИАЛОГА»; **v12-dialog-memory-exception** — исключение вырезано в правиле 3 (сведения о пользователе и ходе диалога — из истории; отказ «такой информации нет» на личные вопросы неверен). Верифицировано на ai-curator и lead-qualification: «А меня как зовут?» → «Тебя зовут Саша». (4) Устаревший маршрут Lead Qualification (лендинг lead-qual-demo переработан 01.09 в продуктовую витрину без интерактива): DEMO_ROUTE.md + SYSTEM_DEMO.md актуализированы в Lead-Qualification-MVP (`97bb4d9`, push) — точка входа веб-форма «Оставить обращение» → бот @OptimusLeadQualificationBot → админ-консоль с демо-входом; sync 243 документа / 7682 чанка; ответ ассистента по текущему потоку. Лендинг на витрине оставлен (решение владельца — первая работа на CSS). Промпт v10→v12 в БД (SOT — БД, история версий сохраняется). Протокол — в документации проекта |
| 2026-09-03 | Morphic-проба реализована и развёрнута (подбор кейса по задаче зрителя) | Экран 1b морфик-сцены: на главной странице форма «опишите свою задачу» → `POST /case-match` → до 3 карточек кейсов с «why»-объяснением применимости. Grounded: retrieval по документам KB + реестр видимых карточек; LLM-объяснение — только по документам (без обещаний статусов и цен); честное «прямого кейса про X нет, ближайший…». Scope пробы (решение владельца): без кеша, без сессий, без визарда; откат — флаг `MORPHIC_ENABLED` на фронте + revert. Инфраструктура: эндпойнт `case_match.py`, миграции 023/024 (инвентарь tech-тегов, синхронизация тегов реестра), прокси `/case-match`, Cache-Control: no-cache для HTML. Верификация Playwright в проде: 6/6 PASS. Протокол — в документации проекта |
| 2026-09-03 | Панель документа в ассистенте + plain-фикс; Admission Console: фикс подвешенного статуса; hero кейсов | (1) **Панель документа:** чип источника с repo+path+excerpt — кнопка; `GET /document-fragment` — фрагмент документа вокруг цитированного чанка с подсветкой (PUA-маркеры → `<mark>`); ветка fail-closed из реестра допуска; raw с raw.githubusercontent.com (кеш TTL 600с/128 док); честный фолбэк при правке документа после индексации. Репорт владельца вскрыл root cause: sync хранит не raw md, а plain-трансформ (`_markdown_to_plain_text`) — панель переведена на то же представление (локализация чанка по построению), в UI подсказка про plain-текст; +4 unit-теста. (2) **Admission Console:** «Есть изменения» подвешивалось навсегда — `has_draft_changes` сравнивал только факт наличия draft-колонок («Обновить состав» всегда пишет черновик), а «Сохранить» гасилась сравнением с preview; фикс: сравнение значений черновика с эффективными правилами + активация «Сохранить» при `patterns_changed`; по данным БД честный статус у 13 из 14 источников (новые файлы в репо), идентичный черновик сброшен у telegram-ai-gateway. Сьют 239 passed. (3) **Hero кейсов:** подсказка «Как проверить демо за 2 минуты» сдвигала скриншоты — `.hero__body` 640→780px, `.demo-hint` → 100% на всех 13 страницах; замеры 1440/1024/768 — подсказка 2 строки, медиа в кадре, оверфлоу не добавлен; hero-лиды HR Assistant и Prompt Review сокращены по запросу владельца. Протокол — в документации проекта |
| 2026-09-03 | Полный комплект документации по стандартам APL + ROADMAP | Сверка комплекта с репозиторием AIC (25 доков) и стандартами лаборатории (three-layer documentation standard, APL documentation design): витринный проект — полный комплект. Новые: ROADMAP (SHOULD S1/S3, COULD C1/C2 ТЗ + нарисовавшееся в разработке), API_CONTRACT (публичные endpoints), USER_GUIDE (посетитель), OPERATIONS (runbook администратора), DEPLOYMENT_VALIDATION_REPORT (публичный протокол DV 02.09 — вынос из внутренней истории), TESTING_CONTRACT (unit/eval-set/E2E). Актуализация: PROJECT_STATE, SPEC, README, PROMPT_ARCHITECTURE (экспорт активного промпта v8 → v12-dialog-memory-exception), IMPLEMENTATION_PLAN, ADMIN_GUIDE, E2E_SCENARIOS (сценарии 03.09), MEDIA_INDEX. LICENSE — по решению владельца пока не добавляется. Эмодзи-контракт, свип внутренних артефактов, garble-свип, перекрёстные ссылки. Коммит/пуш — по команде владельца |


## 📚 11. Связанные документы

- [`README.md`](../README.md) — главная страница проекта.
- [`docs/SPEC.md`](SPEC.md) — продуктовая спецификация v2.3 (as-built baseline).
- [`docs/TZ.md`](TZ.md) — техническое задание v1.7 (утверждено).
- [`docs/IMPLEMENTATION_PLAN.md`](IMPLEMENTATION_PLAN.md) — остаточный технический план v4.20 (от 03.09.2026).
- [`docs/ROADMAP.md`](ROADMAP.md) — роадмап развития после release 04.09.2026.
- [`docs/API_CONTRACT.md`](API_CONTRACT.md) — публичный API-контракт.
- [`docs/USER_GUIDE.md`](USER_GUIDE.md) — руководство посетителя витрины и ассистента.
- [`docs/OPERATIONS.md`](OPERATIONS.md) — runbook администратора.
- [`docs/TESTING_CONTRACT.md`](TESTING_CONTRACT.md) — тестовые контуры и регрессия.
- [`docs/DEPLOYMENT_VALIDATION_REPORT.md`](DEPLOYMENT_VALIDATION_REPORT.md) — протокол Deployment Validation (02.09.2026, PASS).
- [`docs/ARCHITECTURE.md`](ARCHITECTURE.md) — архитектура.
- `docs/DEPLOYMENT_GUIDE.md` — развёртывание. **Существует** (v1.1 от 02.09.2026, коммит `398163a`): создан 02.09 (`f919155`) и валидирован Deployment Validation запуском в чистом окружении (см. §2 «Что не реализовано», пункт Deployment Validation).
- Внутренние аналитические артефакты (рейтинг кандидатов, аудит корпуса и технического долга) — за пределами публичной сборки.
